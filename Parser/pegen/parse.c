// @generated by pegen.py from ./Grammar/python.gram
#include "pegen.h"
const int n_keyword_lists = 9;
KeywordToken *reserved_keywords[] = {
    NULL,
    NULL,
    (KeywordToken[]) {
        {"if", 510},
        {"in", 518},
        {"is", 526},
        {"as", 530},
        {"or", 531},
        {NULL, -1},
    },
    (KeywordToken[]) {
        {"del", 503},
        {"try", 511},
        {"for", 517},
        {"def", 522},
        {"not", 525},
        {"and", 532},
        {NULL, -1},
    },
    (KeywordToken[]) {
        {"pass", 502},
        {"from", 514},
        {"elif", 515},
        {"else", 516},
        {"with", 519},
        {"True", 527},
        {"None", 529},
        {NULL, -1},
    },
    (KeywordToken[]) {
        {"raise", 501},
        {"yield", 504},
        {"break", 506},
        {"while", 512},
        {"class", 523},
        {"False", 528},
        {NULL, -1},
    },
    (KeywordToken[]) {
        {"return", 500},
        {"assert", 505},
        {"global", 508},
        {"import", 513},
        {"except", 520},
        {"lambda", 524},
        {NULL, -1},
    },
    (KeywordToken[]) {
        {"finally", 521},
        {NULL, -1},
    },
    (KeywordToken[]) {
        {"continue", 507},
        {"nonlocal", 509},
        {NULL, -1},
    },
};
#define start_type 1000
#define statements_type 1001
#define statement_type 1002
#define simple_stmt_type 1003
#define small_stmt_type 1004
#define compound_stmt_type 1005
#define assignment_type 1006
#define augassign_type 1007
#define global_stmt_type 1008
#define nonlocal_stmt_type 1009
#define yield_stmt_type 1010
#define assert_stmt_type 1011
#define del_stmt_type 1012
#define import_stmt_type 1013
#define import_name_type 1014
#define import_from_type 1015
#define import_from_targets_type 1016
#define import_from_as_names_type 1017
#define import_from_as_name_type 1018
#define dotted_as_names_type 1019
#define dotted_as_name_type 1020
#define dotted_name_type 1021
#define if_stmt_type 1022
#define elif_stmt_type 1023
#define else_block_type 1024
#define while_stmt_type 1025
#define for_stmt_type 1026
#define with_stmt_type 1027
#define with_item_type 1028
#define try_stmt_type 1029
#define except_block_type 1030
#define finally_block_type 1031
#define return_stmt_type 1032
#define raise_stmt_type 1033
#define function_def_type 1034
#define function_def_raw_type 1035
#define parameters_type 1036
#define slash_without_default_type 1037
#define slash_with_default_type 1038
#define star_etc_type 1039
#define name_with_optional_default_type 1040
#define names_with_default_type 1041
#define name_with_default_type 1042
#define plain_names_type 1043
#define plain_name_type 1044
#define kwds_type 1045
#define annotation_type 1046
#define decorators_type 1047
#define class_def_type 1048
#define class_def_raw_type 1049
#define block_type 1050
#define expressions_list_type 1051
#define expressions_type 1052
#define star_expression_type 1053
#define star_named_expressions_type 1054
#define star_named_expression_type 1055
#define named_expression_type 1056
#define annotated_rhs_type 1057
#define expression_type 1058
#define lambdef_type 1059
#define lambda_parameters_type 1060
#define lambda_slash_without_default_type 1061
#define lambda_slash_with_default_type 1062
#define lambda_star_etc_type 1063
#define lambda_name_with_optional_default_type 1064
#define lambda_names_with_default_type 1065
#define lambda_name_with_default_type 1066
#define lambda_plain_names_type 1067
#define lambda_plain_name_type 1068
#define lambda_kwds_type 1069
#define disjunction_type 1070
#define conjunction_type 1071
#define inversion_type 1072
#define comparison_type 1073
#define compare_op_bitwise_or_pair_type 1074
#define eq_bitwise_or_type 1075
#define noteq_bitwise_or_type 1076
#define lte_bitwise_or_type 1077
#define lt_bitwise_or_type 1078
#define gte_bitwise_or_type 1079
#define gt_bitwise_or_type 1080
#define notin_bitwise_or_type 1081
#define in_bitwise_or_type 1082
#define isnot_bitwise_or_type 1083
#define is_bitwise_or_type 1084
#define bitwise_or_type 1085
#define bitwise_xor_type 1086
#define bitwise_and_type 1087
#define shift_expr_type 1088
#define sum_type 1089
#define term_type 1090
#define factor_type 1091
#define power_type 1092
#define await_primary_type 1093
#define primary_type 1094
#define slices_type 1095
#define slice_type 1096
#define atom_type 1097
#define list_type 1098
#define listcomp_type 1099
#define tuple_type 1100
#define group_type 1101
#define genexp_type 1102
#define set_type 1103
#define setcomp_type 1104
#define dict_type 1105
#define dictcomp_type 1106
#define kvpairs_type 1107
#define kvpair_type 1108
#define for_if_clauses_type 1109
#define yield_expr_type 1110
#define arguments_type 1111
#define args_type 1112
#define kwargs_type 1113
#define starred_expression_type 1114
#define kwarg_type 1115
#define star_targets_type 1116
#define star_targets_seq_type 1117
#define star_target_type 1118
#define star_atom_type 1119
#define inside_paren_ann_assign_target_type 1120
#define ann_assign_subscript_attribute_target_type 1121
#define del_targets_type 1122
#define del_target_type 1123
#define del_t_atom_type 1124
#define targets_type 1125
#define target_type 1126
#define t_primary_type 1127
#define t_lookahead_type 1128
#define t_atom_type 1129
#define _loop1_1_type 1130
#define _loop0_3_type 1131
#define _gather_2_type 1132
#define _tmp_4_type 1133
#define _tmp_5_type 1134
#define _tmp_6_type 1135
#define _tmp_7_type 1136
#define _tmp_8_type 1137
#define _tmp_9_type 1138
#define _tmp_10_type 1139
#define _tmp_11_type 1140
#define _loop1_12_type 1141
#define _tmp_13_type 1142
#define _tmp_14_type 1143
#define _loop0_16_type 1144
#define _gather_15_type 1145
#define _loop0_18_type 1146
#define _gather_17_type 1147
#define _tmp_19_type 1148
#define _loop0_20_type 1149
#define _loop1_21_type 1150
#define _loop0_23_type 1151
#define _gather_22_type 1152
#define _tmp_24_type 1153
#define _loop0_26_type 1154
#define _gather_25_type 1155
#define _tmp_27_type 1156
#define _loop0_29_type 1157
#define _gather_28_type 1158
#define _loop0_31_type 1159
#define _gather_30_type 1160
#define _loop0_33_type 1161
#define _gather_32_type 1162
#define _loop0_35_type 1163
#define _gather_34_type 1164
#define _tmp_36_type 1165
#define _loop1_37_type 1166
#define _tmp_38_type 1167
#define _tmp_39_type 1168
#define _tmp_40_type 1169
#define _tmp_41_type 1170
#define _tmp_42_type 1171
#define _tmp_43_type 1172
#define _tmp_44_type 1173
#define _tmp_45_type 1174
#define _tmp_46_type 1175
#define _tmp_47_type 1176
#define _tmp_48_type 1177
#define _tmp_49_type 1178
#define _tmp_50_type 1179
#define _loop0_51_type 1180
#define _tmp_52_type 1181
#define _loop1_53_type 1182
#define _tmp_54_type 1183
#define _tmp_55_type 1184
#define _loop0_57_type 1185
#define _gather_56_type 1186
#define _loop0_59_type 1187
#define _gather_58_type 1188
#define _tmp_60_type 1189
#define _loop1_61_type 1190
#define _tmp_62_type 1191
#define _loop0_64_type 1192
#define _gather_63_type 1193
#define _loop1_65_type 1194
#define _loop0_67_type 1195
#define _gather_66_type 1196
#define _tmp_68_type 1197
#define _tmp_69_type 1198
#define _tmp_70_type 1199
#define _tmp_71_type 1200
#define _tmp_72_type 1201
#define _tmp_73_type 1202
#define _tmp_74_type 1203
#define _tmp_75_type 1204
#define _tmp_76_type 1205
#define _loop0_77_type 1206
#define _tmp_78_type 1207
#define _loop1_79_type 1208
#define _tmp_80_type 1209
#define _tmp_81_type 1210
#define _loop0_83_type 1211
#define _gather_82_type 1212
#define _loop0_85_type 1213
#define _gather_84_type 1214
#define _loop1_86_type 1215
#define _loop1_87_type 1216
#define _loop1_88_type 1217
#define _loop0_90_type 1218
#define _gather_89_type 1219
#define _tmp_91_type 1220
#define _loop1_92_type 1221
#define _tmp_93_type 1222
#define _tmp_94_type 1223
#define _tmp_95_type 1224
#define _tmp_96_type 1225
#define _tmp_97_type 1226
#define _loop0_99_type 1227
#define _gather_98_type 1228
#define _loop1_100_type 1229
#define _tmp_101_type 1230
#define _tmp_102_type 1231
#define _loop0_104_type 1232
#define _gather_103_type 1233
#define _loop0_105_type 1234
#define _loop0_107_type 1235
#define _gather_106_type 1236
#define _loop0_109_type 1237
#define _gather_108_type 1238
#define _loop0_111_type 1239
#define _gather_110_type 1240
#define _tmp_112_type 1241
#define _tmp_113_type 1242
#define _tmp_114_type 1243
#define _tmp_115_type 1244
#define _tmp_116_type 1245
#define _tmp_117_type 1246
#define _tmp_118_type 1247
#define _tmp_119_type 1248
#define _tmp_120_type 1249
#define _tmp_121_type 1250
#define _tmp_122_type 1251
#define _loop0_123_type 1252
#define _tmp_124_type 1253

static mod_ty start_rule(Parser *p);
static asdl_seq* statements_rule(Parser *p);
static asdl_seq* statement_rule(Parser *p);
static asdl_seq* simple_stmt_rule(Parser *p);
static stmt_ty small_stmt_rule(Parser *p);
static stmt_ty compound_stmt_rule(Parser *p);
static void *assignment_rule(Parser *p);
static AugOperator* augassign_rule(Parser *p);
static stmt_ty global_stmt_rule(Parser *p);
static stmt_ty nonlocal_stmt_rule(Parser *p);
static stmt_ty yield_stmt_rule(Parser *p);
static stmt_ty assert_stmt_rule(Parser *p);
static stmt_ty del_stmt_rule(Parser *p);
static stmt_ty import_stmt_rule(Parser *p);
static stmt_ty import_name_rule(Parser *p);
static stmt_ty import_from_rule(Parser *p);
static asdl_seq* import_from_targets_rule(Parser *p);
static asdl_seq* import_from_as_names_rule(Parser *p);
static alias_ty import_from_as_name_rule(Parser *p);
static asdl_seq* dotted_as_names_rule(Parser *p);
static alias_ty dotted_as_name_rule(Parser *p);
static expr_ty dotted_name_rule(Parser *p);
static stmt_ty if_stmt_rule(Parser *p);
static stmt_ty elif_stmt_rule(Parser *p);
static asdl_seq* else_block_rule(Parser *p);
static stmt_ty while_stmt_rule(Parser *p);
static stmt_ty for_stmt_rule(Parser *p);
static stmt_ty with_stmt_rule(Parser *p);
static withitem_ty with_item_rule(Parser *p);
static stmt_ty try_stmt_rule(Parser *p);
static excepthandler_ty except_block_rule(Parser *p);
static asdl_seq* finally_block_rule(Parser *p);
static stmt_ty return_stmt_rule(Parser *p);
static stmt_ty raise_stmt_rule(Parser *p);
static stmt_ty function_def_rule(Parser *p);
static stmt_ty function_def_raw_rule(Parser *p);
static arguments_ty parameters_rule(Parser *p);
static asdl_seq* slash_without_default_rule(Parser *p);
static SlashWithDefault* slash_with_default_rule(Parser *p);
static StarEtc* star_etc_rule(Parser *p);
static NameDefaultPair* name_with_optional_default_rule(Parser *p);
static asdl_seq* names_with_default_rule(Parser *p);
static NameDefaultPair* name_with_default_rule(Parser *p);
static asdl_seq* plain_names_rule(Parser *p);
static arg_ty plain_name_rule(Parser *p);
static arg_ty kwds_rule(Parser *p);
static expr_ty annotation_rule(Parser *p);
static asdl_seq* decorators_rule(Parser *p);
static stmt_ty class_def_rule(Parser *p);
static stmt_ty class_def_raw_rule(Parser *p);
static asdl_seq* block_rule(Parser *p);
static asdl_seq* expressions_list_rule(Parser *p);
static expr_ty expressions_rule(Parser *p);
static expr_ty star_expression_rule(Parser *p);
static asdl_seq* star_named_expressions_rule(Parser *p);
static expr_ty star_named_expression_rule(Parser *p);
static expr_ty named_expression_rule(Parser *p);
static expr_ty annotated_rhs_rule(Parser *p);
static expr_ty expression_rule(Parser *p);
static expr_ty lambdef_rule(Parser *p);
static arguments_ty lambda_parameters_rule(Parser *p);
static asdl_seq* lambda_slash_without_default_rule(Parser *p);
static SlashWithDefault* lambda_slash_with_default_rule(Parser *p);
static StarEtc* lambda_star_etc_rule(Parser *p);
static NameDefaultPair* lambda_name_with_optional_default_rule(Parser *p);
static asdl_seq* lambda_names_with_default_rule(Parser *p);
static NameDefaultPair* lambda_name_with_default_rule(Parser *p);
static asdl_seq* lambda_plain_names_rule(Parser *p);
static arg_ty lambda_plain_name_rule(Parser *p);
static arg_ty lambda_kwds_rule(Parser *p);
static expr_ty disjunction_rule(Parser *p);
static expr_ty conjunction_rule(Parser *p);
static expr_ty inversion_rule(Parser *p);
static expr_ty comparison_rule(Parser *p);
static CmpopExprPair* compare_op_bitwise_or_pair_rule(Parser *p);
static CmpopExprPair* eq_bitwise_or_rule(Parser *p);
static CmpopExprPair* noteq_bitwise_or_rule(Parser *p);
static CmpopExprPair* lte_bitwise_or_rule(Parser *p);
static CmpopExprPair* lt_bitwise_or_rule(Parser *p);
static CmpopExprPair* gte_bitwise_or_rule(Parser *p);
static CmpopExprPair* gt_bitwise_or_rule(Parser *p);
static CmpopExprPair* notin_bitwise_or_rule(Parser *p);
static CmpopExprPair* in_bitwise_or_rule(Parser *p);
static CmpopExprPair* isnot_bitwise_or_rule(Parser *p);
static CmpopExprPair* is_bitwise_or_rule(Parser *p);
static expr_ty bitwise_or_rule(Parser *p);
static expr_ty bitwise_xor_rule(Parser *p);
static expr_ty bitwise_and_rule(Parser *p);
static expr_ty shift_expr_rule(Parser *p);
static expr_ty sum_rule(Parser *p);
static expr_ty term_rule(Parser *p);
static expr_ty factor_rule(Parser *p);
static expr_ty power_rule(Parser *p);
static expr_ty await_primary_rule(Parser *p);
static expr_ty primary_rule(Parser *p);
static expr_ty slices_rule(Parser *p);
static expr_ty slice_rule(Parser *p);
static expr_ty atom_rule(Parser *p);
static expr_ty list_rule(Parser *p);
static expr_ty listcomp_rule(Parser *p);
static expr_ty tuple_rule(Parser *p);
static expr_ty group_rule(Parser *p);
static expr_ty genexp_rule(Parser *p);
static expr_ty set_rule(Parser *p);
static expr_ty setcomp_rule(Parser *p);
static expr_ty dict_rule(Parser *p);
static expr_ty dictcomp_rule(Parser *p);
static asdl_seq* kvpairs_rule(Parser *p);
static KeyValuePair* kvpair_rule(Parser *p);
static asdl_seq* for_if_clauses_rule(Parser *p);
static expr_ty yield_expr_rule(Parser *p);
static expr_ty arguments_rule(Parser *p);
static expr_ty args_rule(Parser *p);
static asdl_seq* kwargs_rule(Parser *p);
static expr_ty starred_expression_rule(Parser *p);
static KeywordOrStarred* kwarg_rule(Parser *p);
static expr_ty star_targets_rule(Parser *p);
static asdl_seq* star_targets_seq_rule(Parser *p);
static expr_ty star_target_rule(Parser *p);
static expr_ty star_atom_rule(Parser *p);
static expr_ty inside_paren_ann_assign_target_rule(Parser *p);
static expr_ty ann_assign_subscript_attribute_target_rule(Parser *p);
static asdl_seq* del_targets_rule(Parser *p);
static expr_ty del_target_rule(Parser *p);
static expr_ty del_t_atom_rule(Parser *p);
static asdl_seq* targets_rule(Parser *p);
static expr_ty target_rule(Parser *p);
static expr_ty t_primary_rule(Parser *p);
static void *t_lookahead_rule(Parser *p);
static expr_ty t_atom_rule(Parser *p);
static asdl_seq *_loop1_1_rule(Parser *p);
static asdl_seq *_loop0_3_rule(Parser *p);
static asdl_seq *_gather_2_rule(Parser *p);
static void *_tmp_4_rule(Parser *p);
static void *_tmp_5_rule(Parser *p);
static void *_tmp_6_rule(Parser *p);
static void *_tmp_7_rule(Parser *p);
static void *_tmp_8_rule(Parser *p);
static void *_tmp_9_rule(Parser *p);
static void *_tmp_10_rule(Parser *p);
static void *_tmp_11_rule(Parser *p);
static asdl_seq *_loop1_12_rule(Parser *p);
static void *_tmp_13_rule(Parser *p);
static void *_tmp_14_rule(Parser *p);
static asdl_seq *_loop0_16_rule(Parser *p);
static asdl_seq *_gather_15_rule(Parser *p);
static asdl_seq *_loop0_18_rule(Parser *p);
static asdl_seq *_gather_17_rule(Parser *p);
static void *_tmp_19_rule(Parser *p);
static asdl_seq *_loop0_20_rule(Parser *p);
static asdl_seq *_loop1_21_rule(Parser *p);
static asdl_seq *_loop0_23_rule(Parser *p);
static asdl_seq *_gather_22_rule(Parser *p);
static void *_tmp_24_rule(Parser *p);
static asdl_seq *_loop0_26_rule(Parser *p);
static asdl_seq *_gather_25_rule(Parser *p);
static void *_tmp_27_rule(Parser *p);
static asdl_seq *_loop0_29_rule(Parser *p);
static asdl_seq *_gather_28_rule(Parser *p);
static asdl_seq *_loop0_31_rule(Parser *p);
static asdl_seq *_gather_30_rule(Parser *p);
static asdl_seq *_loop0_33_rule(Parser *p);
static asdl_seq *_gather_32_rule(Parser *p);
static asdl_seq *_loop0_35_rule(Parser *p);
static asdl_seq *_gather_34_rule(Parser *p);
static void *_tmp_36_rule(Parser *p);
static asdl_seq *_loop1_37_rule(Parser *p);
static void *_tmp_38_rule(Parser *p);
static void *_tmp_39_rule(Parser *p);
static void *_tmp_40_rule(Parser *p);
static void *_tmp_41_rule(Parser *p);
static void *_tmp_42_rule(Parser *p);
static void *_tmp_43_rule(Parser *p);
static void *_tmp_44_rule(Parser *p);
static void *_tmp_45_rule(Parser *p);
static void *_tmp_46_rule(Parser *p);
static void *_tmp_47_rule(Parser *p);
static void *_tmp_48_rule(Parser *p);
static void *_tmp_49_rule(Parser *p);
static void *_tmp_50_rule(Parser *p);
static asdl_seq *_loop0_51_rule(Parser *p);
static void *_tmp_52_rule(Parser *p);
static asdl_seq *_loop1_53_rule(Parser *p);
static void *_tmp_54_rule(Parser *p);
static void *_tmp_55_rule(Parser *p);
static asdl_seq *_loop0_57_rule(Parser *p);
static asdl_seq *_gather_56_rule(Parser *p);
static asdl_seq *_loop0_59_rule(Parser *p);
static asdl_seq *_gather_58_rule(Parser *p);
static void *_tmp_60_rule(Parser *p);
static asdl_seq *_loop1_61_rule(Parser *p);
static void *_tmp_62_rule(Parser *p);
static asdl_seq *_loop0_64_rule(Parser *p);
static asdl_seq *_gather_63_rule(Parser *p);
static asdl_seq *_loop1_65_rule(Parser *p);
static asdl_seq *_loop0_67_rule(Parser *p);
static asdl_seq *_gather_66_rule(Parser *p);
static void *_tmp_68_rule(Parser *p);
static void *_tmp_69_rule(Parser *p);
static void *_tmp_70_rule(Parser *p);
static void *_tmp_71_rule(Parser *p);
static void *_tmp_72_rule(Parser *p);
static void *_tmp_73_rule(Parser *p);
static void *_tmp_74_rule(Parser *p);
static void *_tmp_75_rule(Parser *p);
static void *_tmp_76_rule(Parser *p);
static asdl_seq *_loop0_77_rule(Parser *p);
static void *_tmp_78_rule(Parser *p);
static asdl_seq *_loop1_79_rule(Parser *p);
static void *_tmp_80_rule(Parser *p);
static void *_tmp_81_rule(Parser *p);
static asdl_seq *_loop0_83_rule(Parser *p);
static asdl_seq *_gather_82_rule(Parser *p);
static asdl_seq *_loop0_85_rule(Parser *p);
static asdl_seq *_gather_84_rule(Parser *p);
static asdl_seq *_loop1_86_rule(Parser *p);
static asdl_seq *_loop1_87_rule(Parser *p);
static asdl_seq *_loop1_88_rule(Parser *p);
static asdl_seq *_loop0_90_rule(Parser *p);
static asdl_seq *_gather_89_rule(Parser *p);
static void *_tmp_91_rule(Parser *p);
static asdl_seq *_loop1_92_rule(Parser *p);
static void *_tmp_93_rule(Parser *p);
static void *_tmp_94_rule(Parser *p);
static void *_tmp_95_rule(Parser *p);
static void *_tmp_96_rule(Parser *p);
static void *_tmp_97_rule(Parser *p);
static asdl_seq *_loop0_99_rule(Parser *p);
static asdl_seq *_gather_98_rule(Parser *p);
static asdl_seq *_loop1_100_rule(Parser *p);
static void *_tmp_101_rule(Parser *p);
static void *_tmp_102_rule(Parser *p);
static asdl_seq *_loop0_104_rule(Parser *p);
static asdl_seq *_gather_103_rule(Parser *p);
static asdl_seq *_loop0_105_rule(Parser *p);
static asdl_seq *_loop0_107_rule(Parser *p);
static asdl_seq *_gather_106_rule(Parser *p);
static asdl_seq *_loop0_109_rule(Parser *p);
static asdl_seq *_gather_108_rule(Parser *p);
static asdl_seq *_loop0_111_rule(Parser *p);
static asdl_seq *_gather_110_rule(Parser *p);
static void *_tmp_112_rule(Parser *p);
static void *_tmp_113_rule(Parser *p);
static void *_tmp_114_rule(Parser *p);
static void *_tmp_115_rule(Parser *p);
static void *_tmp_116_rule(Parser *p);
static void *_tmp_117_rule(Parser *p);
static void *_tmp_118_rule(Parser *p);
static void *_tmp_119_rule(Parser *p);
static void *_tmp_120_rule(Parser *p);
static void *_tmp_121_rule(Parser *p);
static void *_tmp_122_rule(Parser *p);
static asdl_seq *_loop0_123_rule(Parser *p);
static void *_tmp_124_rule(Parser *p);


// start: statements? $
static mod_ty
start_rule(Parser *p)
{
    mod_ty res = NULL;
    if (is_memoized(p, start_type, &res))
        return res;
    int mark = p->mark;
    { // statements? $
        void *a;
        void *endmarker_var;
        if (
            (a = statements_rule(p), 1)
            &&
            (endmarker_var = endmarker_token(p))
        )
        {
            res = Module ( a , NULL , p -> arena );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, start_type, res);
    return res;
}

// statements: statement+
static asdl_seq*
statements_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, statements_type, &res))
        return res;
    int mark = p->mark;
    { // statement+
        asdl_seq * a;
        if (
            (a = _loop1_1_rule(p))
        )
        {
            res = seq_flatten ( p , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, statements_type, res);
    return res;
}

// statement: compound_stmt | simple_stmt
static asdl_seq*
statement_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, statement_type, &res))
        return res;
    int mark = p->mark;
    { // compound_stmt
        stmt_ty a;
        if (
            (a = compound_stmt_rule(p))
        )
        {
            res = singleton_seq ( p , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // simple_stmt
        asdl_seq* simple_stmt_var;
        if (
            (simple_stmt_var = simple_stmt_rule(p))
        )
        {
            res = simple_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, statement_type, res);
    return res;
}

// simple_stmt: small_stmt !';' NEWLINE | ';'.small_stmt+ ';'? NEWLINE
static asdl_seq*
simple_stmt_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, simple_stmt_type, &res))
        return res;
    int mark = p->mark;
    { // small_stmt !';' NEWLINE
        stmt_ty a;
        void *newline_var;
        if (
            (a = small_stmt_rule(p))
            &&
            lookahead_with_int(0, expect_token, p, 13)
            &&
            (newline_var = newline_token(p))
        )
        {
            res = singleton_seq ( p , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // ';'.small_stmt+ ';'? NEWLINE
        asdl_seq * a;
        void *newline_var;
        void *opt_var;
        if (
            (a = _gather_2_rule(p))
            &&
            (opt_var = expect_token(p, 13), 1)
            &&
            (newline_var = newline_token(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, simple_stmt_type, res);
    return res;
}

// small_stmt:
//     | assignment
//     | expressions
//     | &'return' return_stmt
//     | &('import' | 'from') import_stmt
//     | &'raise' raise_stmt
//     | 'pass'
//     | &'del' del_stmt
//     | &'yield' yield_stmt
//     | &'assert' assert_stmt
//     | 'break'
//     | 'continue'
//     | &'global' global_stmt
//     | &'nonlocal' nonlocal_stmt
static stmt_ty
small_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, small_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // assignment
        void *assignment_var;
        if (
            (assignment_var = assignment_rule(p))
        )
        {
            res = assignment_var;
            goto done;
        }
        p->mark = mark;
    }
    { // expressions
        expr_ty e;
        if (
            (e = expressions_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Expr ( e , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // &'return' return_stmt
        stmt_ty return_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 500)
            &&
            (return_stmt_var = return_stmt_rule(p))
        )
        {
            res = return_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &('import' | 'from') import_stmt
        stmt_ty import_stmt_var;
        if (
            lookahead(1, _tmp_4_rule, p)
            &&
            (import_stmt_var = import_stmt_rule(p))
        )
        {
            res = import_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'raise' raise_stmt
        stmt_ty raise_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 501)
            &&
            (raise_stmt_var = raise_stmt_rule(p))
        )
        {
            res = raise_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // 'pass'
        void *keyword;
        if (
            (keyword = expect_token(p, 502))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Pass ( EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // &'del' del_stmt
        stmt_ty del_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 503)
            &&
            (del_stmt_var = del_stmt_rule(p))
        )
        {
            res = del_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'yield' yield_stmt
        stmt_ty yield_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 504)
            &&
            (yield_stmt_var = yield_stmt_rule(p))
        )
        {
            res = yield_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'assert' assert_stmt
        stmt_ty assert_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 505)
            &&
            (assert_stmt_var = assert_stmt_rule(p))
        )
        {
            res = assert_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // 'break'
        void *keyword;
        if (
            (keyword = expect_token(p, 506))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Break ( EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'continue'
        void *keyword;
        if (
            (keyword = expect_token(p, 507))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Continue ( EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // &'global' global_stmt
        stmt_ty global_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 508)
            &&
            (global_stmt_var = global_stmt_rule(p))
        )
        {
            res = global_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'nonlocal' nonlocal_stmt
        stmt_ty nonlocal_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 509)
            &&
            (nonlocal_stmt_var = nonlocal_stmt_rule(p))
        )
        {
            res = nonlocal_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, small_stmt_type, res);
    return res;
}

// compound_stmt:
//     | &('def' | '@' | ASYNC) function_def
//     | &'if' if_stmt
//     | &('class' | '@') class_def
//     | &('with' | ASYNC) with_stmt
//     | &('for' | ASYNC) for_stmt
//     | &'try' try_stmt
//     | &'while' while_stmt
static stmt_ty
compound_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, compound_stmt_type, &res))
        return res;
    int mark = p->mark;
    { // &('def' | '@' | ASYNC) function_def
        stmt_ty function_def_var;
        if (
            lookahead(1, _tmp_5_rule, p)
            &&
            (function_def_var = function_def_rule(p))
        )
        {
            res = function_def_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'if' if_stmt
        stmt_ty if_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 510)
            &&
            (if_stmt_var = if_stmt_rule(p))
        )
        {
            res = if_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &('class' | '@') class_def
        stmt_ty class_def_var;
        if (
            lookahead(1, _tmp_6_rule, p)
            &&
            (class_def_var = class_def_rule(p))
        )
        {
            res = class_def_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &('with' | ASYNC) with_stmt
        stmt_ty with_stmt_var;
        if (
            lookahead(1, _tmp_7_rule, p)
            &&
            (with_stmt_var = with_stmt_rule(p))
        )
        {
            res = with_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &('for' | ASYNC) for_stmt
        stmt_ty for_stmt_var;
        if (
            lookahead(1, _tmp_8_rule, p)
            &&
            (for_stmt_var = for_stmt_rule(p))
        )
        {
            res = for_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'try' try_stmt
        stmt_ty try_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 511)
            &&
            (try_stmt_var = try_stmt_rule(p))
        )
        {
            res = try_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'while' while_stmt
        stmt_ty while_stmt_var;
        if (
            lookahead_with_int(1, expect_token, p, 512)
            &&
            (while_stmt_var = while_stmt_rule(p))
        )
        {
            res = while_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, compound_stmt_type, res);
    return res;
}

// assignment:
//     | NAME ':' expression ['=' annotated_rhs]
//     | ('(' inside_paren_ann_assign_target ')' | ann_assign_subscript_attribute_target) ':' expression ['=' annotated_rhs]
//     | ((star_targets '='))+ (yield_expr | expressions)
//     | target augassign (yield_expr | expressions)
static void *
assignment_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, assignment_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME ':' expression ['=' annotated_rhs]
        expr_ty a;
        expr_ty b;
        void *c;
        void *literal;
        if (
            (a = name_token(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = expression_rule(p))
            &&
            (c = _tmp_9_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_AnnAssign ( CHECK ( set_expr_context ( p , a , Store ) ) , b , c , 1 , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // ('(' inside_paren_ann_assign_target ')' | ann_assign_subscript_attribute_target) ':' expression ['=' annotated_rhs]
        void *a;
        expr_ty b;
        void *c;
        void *literal;
        if (
            (a = _tmp_10_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = expression_rule(p))
            &&
            (c = _tmp_11_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_AnnAssign ( a , b , c , 0 , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // ((star_targets '='))+ (yield_expr | expressions)
        asdl_seq * a;
        void *b;
        if (
            (a = _loop1_12_rule(p))
            &&
            (b = _tmp_13_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Assign ( a , b , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // target augassign (yield_expr | expressions)
        expr_ty a;
        AugOperator* b;
        void *c;
        if (
            (a = target_rule(p))
            &&
            (b = augassign_rule(p))
            &&
            (c = _tmp_14_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_AugAssign ( a , b -> kind , c , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, assignment_type, res);
    return res;
}

// augassign:
//     | '+='
//     | '-='
//     | '*='
//     | '@='
//     | '/='
//     | '%='
//     | '&='
//     | '|='
//     | '^='
//     | '<<='
//     | '>>='
//     | '**='
//     | '//='
static AugOperator*
augassign_rule(Parser *p)
{
    AugOperator* res = NULL;
    if (is_memoized(p, augassign_type, &res))
        return res;
    int mark = p->mark;
    { // '+='
        void *literal;
        if (
            (literal = expect_token(p, 36))
        )
        {
            res = augoperator ( p , Add );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '-='
        void *literal;
        if (
            (literal = expect_token(p, 37))
        )
        {
            res = augoperator ( p , Sub );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '*='
        void *literal;
        if (
            (literal = expect_token(p, 38))
        )
        {
            res = augoperator ( p , Mult );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '@='
        void *literal;
        if (
            (literal = expect_token(p, 50))
        )
        {
            res = augoperator ( p , MatMult );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '/='
        void *literal;
        if (
            (literal = expect_token(p, 39))
        )
        {
            res = augoperator ( p , Div );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '%='
        void *literal;
        if (
            (literal = expect_token(p, 40))
        )
        {
            res = augoperator ( p , Mod );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '&='
        void *literal;
        if (
            (literal = expect_token(p, 41))
        )
        {
            res = augoperator ( p , BitAnd );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '|='
        void *literal;
        if (
            (literal = expect_token(p, 42))
        )
        {
            res = augoperator ( p , BitOr );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '^='
        void *literal;
        if (
            (literal = expect_token(p, 43))
        )
        {
            res = augoperator ( p , BitXor );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '<<='
        void *literal;
        if (
            (literal = expect_token(p, 44))
        )
        {
            res = augoperator ( p , LShift );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '>>='
        void *literal;
        if (
            (literal = expect_token(p, 45))
        )
        {
            res = augoperator ( p , RShift );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '**='
        void *literal;
        if (
            (literal = expect_token(p, 46))
        )
        {
            res = augoperator ( p , Pow );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '//='
        void *literal;
        if (
            (literal = expect_token(p, 48))
        )
        {
            res = augoperator ( p , FloorDiv );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, augassign_type, res);
    return res;
}

// global_stmt: 'global' ','.NAME+
static stmt_ty
global_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, global_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'global' ','.NAME+
        asdl_seq * a;
        void *keyword;
        if (
            (keyword = expect_token(p, 508))
            &&
            (a = _gather_15_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Global ( CHECK ( map_names_to_ids ( p , a ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, global_stmt_type, res);
    return res;
}

// nonlocal_stmt: 'nonlocal' ','.NAME+
static stmt_ty
nonlocal_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, nonlocal_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'nonlocal' ','.NAME+
        asdl_seq * a;
        void *keyword;
        if (
            (keyword = expect_token(p, 509))
            &&
            (a = _gather_17_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Nonlocal ( CHECK ( map_names_to_ids ( p , a ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, nonlocal_stmt_type, res);
    return res;
}

// yield_stmt: yield_expr
static stmt_ty
yield_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, yield_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // yield_expr
        expr_ty y;
        if (
            (y = yield_expr_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Expr ( y , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, yield_stmt_type, res);
    return res;
}

// assert_stmt: 'assert' expression [',' expression]
static stmt_ty
assert_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, assert_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'assert' expression [',' expression]
        expr_ty a;
        void *b;
        void *keyword;
        if (
            (keyword = expect_token(p, 505))
            &&
            (a = expression_rule(p))
            &&
            (b = _tmp_19_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Assert ( a , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, assert_stmt_type, res);
    return res;
}

// del_stmt: 'del' del_targets
static stmt_ty
del_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, del_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'del' del_targets
        asdl_seq* a;
        void *keyword;
        if (
            (keyword = expect_token(p, 503))
            &&
            (a = del_targets_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Delete ( a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, del_stmt_type, res);
    return res;
}

// import_stmt: import_name | import_from
static stmt_ty
import_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, import_stmt_type, &res))
        return res;
    int mark = p->mark;
    { // import_name
        stmt_ty import_name_var;
        if (
            (import_name_var = import_name_rule(p))
        )
        {
            res = import_name_var;
            goto done;
        }
        p->mark = mark;
    }
    { // import_from
        stmt_ty import_from_var;
        if (
            (import_from_var = import_from_rule(p))
        )
        {
            res = import_from_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, import_stmt_type, res);
    return res;
}

// import_name: 'import' dotted_as_names
static stmt_ty
import_name_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, import_name_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'import' dotted_as_names
        asdl_seq* a;
        void *keyword;
        if (
            (keyword = expect_token(p, 513))
            &&
            (a = dotted_as_names_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Import ( a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, import_name_type, res);
    return res;
}

// import_from:
//     | 'from' (('.' | '...'))* dotted_name 'import' import_from_targets
//     | 'from' (('.' | '...'))+ 'import' import_from_targets
static stmt_ty
import_from_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, import_from_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'from' (('.' | '...'))* dotted_name 'import' import_from_targets
        asdl_seq * a;
        expr_ty b;
        asdl_seq* c;
        void *keyword;
        void *keyword_1;
        if (
            (keyword = expect_token(p, 514))
            &&
            (a = _loop0_20_rule(p))
            &&
            (b = dotted_name_rule(p))
            &&
            (keyword_1 = expect_token(p, 513))
            &&
            (c = import_from_targets_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_ImportFrom ( b -> v . Name . id , c , seq_count_dots ( a ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'from' (('.' | '...'))+ 'import' import_from_targets
        asdl_seq * a;
        asdl_seq* b;
        void *keyword;
        void *keyword_1;
        if (
            (keyword = expect_token(p, 514))
            &&
            (a = _loop1_21_rule(p))
            &&
            (keyword_1 = expect_token(p, 513))
            &&
            (b = import_from_targets_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_ImportFrom ( NULL , b , seq_count_dots ( a ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, import_from_type, res);
    return res;
}

// import_from_targets: '(' import_from_as_names ')' | import_from_as_names | '*'
static asdl_seq*
import_from_targets_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, import_from_targets_type, &res))
        return res;
    int mark = p->mark;
    { // '(' import_from_as_names ')'
        asdl_seq* a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = import_from_as_names_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // import_from_as_names
        asdl_seq* import_from_as_names_var;
        if (
            (import_from_as_names_var = import_from_as_names_rule(p))
        )
        {
            res = import_from_as_names_var;
            goto done;
        }
        p->mark = mark;
    }
    { // '*'
        void *literal;
        if (
            (literal = expect_token(p, 16))
        )
        {
            res = singleton_seq ( p , CHECK ( alias_for_star ( p ) ) );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, import_from_targets_type, res);
    return res;
}

// import_from_as_names: ','.import_from_as_name+ ','?
static asdl_seq*
import_from_as_names_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, import_from_as_names_type, &res))
        return res;
    int mark = p->mark;
    { // ','.import_from_as_name+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_22_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, import_from_as_names_type, res);
    return res;
}

// import_from_as_name: NAME ['as' NAME]
static alias_ty
import_from_as_name_rule(Parser *p)
{
    alias_ty res = NULL;
    if (is_memoized(p, import_from_as_name_type, &res))
        return res;
    int mark = p->mark;
    { // NAME ['as' NAME]
        expr_ty a;
        void *b;
        if (
            (a = name_token(p))
            &&
            (b = _tmp_24_rule(p), 1)
        )
        {
            res = _Py_alias ( a -> v . Name . id , ( b ) ? ( ( expr_ty ) b ) -> v . Name . id : NULL , p -> arena );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, import_from_as_name_type, res);
    return res;
}

// dotted_as_names: ','.dotted_as_name+
static asdl_seq*
dotted_as_names_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, dotted_as_names_type, &res))
        return res;
    int mark = p->mark;
    { // ','.dotted_as_name+
        asdl_seq * a;
        if (
            (a = _gather_25_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, dotted_as_names_type, res);
    return res;
}

// dotted_as_name: dotted_name ['as' NAME]
static alias_ty
dotted_as_name_rule(Parser *p)
{
    alias_ty res = NULL;
    if (is_memoized(p, dotted_as_name_type, &res))
        return res;
    int mark = p->mark;
    { // dotted_name ['as' NAME]
        expr_ty a;
        void *b;
        if (
            (a = dotted_name_rule(p))
            &&
            (b = _tmp_27_rule(p), 1)
        )
        {
            res = _Py_alias ( a -> v . Name . id , ( b ) ? ( ( expr_ty ) b ) -> v . Name . id : NULL , p -> arena );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, dotted_as_name_type, res);
    return res;
}

// dotted_name: dotted_name '.' NAME | NAME
static expr_ty dotted_name_raw(Parser *);
static expr_ty
dotted_name_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, dotted_name_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_0 = update_memo(p, mark, dotted_name_type, res);
        if (tmpvar_0) {
            return res;
        }
        p->mark = mark;
        void *raw = dotted_name_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
dotted_name_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    { // dotted_name '.' NAME
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = dotted_name_rule(p))
            &&
            (literal = expect_token(p, 23))
            &&
            (b = name_token(p))
        )
        {
            res = join_names_with_dot ( p , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // NAME
        expr_ty name_var;
        if (
            (name_var = name_token(p))
        )
        {
            res = name_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// if_stmt:
//     | 'if' named_expression ':' block elif_stmt
//     | 'if' named_expression ':' block else_block?
static stmt_ty
if_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, if_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'if' named_expression ':' block elif_stmt
        expr_ty a;
        asdl_seq* b;
        stmt_ty c;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 510))
            &&
            (a = named_expression_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (c = elif_stmt_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_If ( a , b , CHECK ( singleton_seq ( p , c ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'if' named_expression ':' block else_block?
        expr_ty a;
        asdl_seq* b;
        void *c;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 510))
            &&
            (a = named_expression_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (c = else_block_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_If ( a , b , c , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, if_stmt_type, res);
    return res;
}

// elif_stmt:
//     | 'elif' named_expression ':' block elif_stmt
//     | 'elif' named_expression ':' block else_block?
static stmt_ty
elif_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, elif_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'elif' named_expression ':' block elif_stmt
        expr_ty a;
        asdl_seq* b;
        stmt_ty c;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 515))
            &&
            (a = named_expression_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (c = elif_stmt_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_If ( a , b , CHECK ( singleton_seq ( p , c ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'elif' named_expression ':' block else_block?
        expr_ty a;
        asdl_seq* b;
        void *c;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 515))
            &&
            (a = named_expression_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (c = else_block_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_If ( a , b , c , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, elif_stmt_type, res);
    return res;
}

// else_block: 'else' ':' block
static asdl_seq*
else_block_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, else_block_type, &res))
        return res;
    int mark = p->mark;
    { // 'else' ':' block
        asdl_seq* b;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 516))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            res = b;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, else_block_type, res);
    return res;
}

// while_stmt: 'while' named_expression ':' block else_block?
static stmt_ty
while_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, while_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'while' named_expression ':' block else_block?
        expr_ty a;
        asdl_seq* b;
        void *c;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 512))
            &&
            (a = named_expression_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (c = else_block_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_While ( a , b , c , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, while_stmt_type, res);
    return res;
}

// for_stmt:
//     | ASYNC 'for' star_targets 'in' expressions ':' block else_block?
//     | 'for' star_targets 'in' expressions ':' block else_block?
static stmt_ty
for_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, for_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // ASYNC 'for' star_targets 'in' expressions ':' block else_block?
        void *async_var;
        asdl_seq* b;
        void *el;
        expr_ty ex;
        void *keyword;
        void *keyword_1;
        void *literal;
        expr_ty t;
        if (
            (async_var = async_token(p))
            &&
            (keyword = expect_token(p, 517))
            &&
            (t = star_targets_rule(p))
            &&
            (keyword_1 = expect_token(p, 518))
            &&
            (ex = expressions_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (el = else_block_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_AsyncFor ( t , ex , b , el , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'for' star_targets 'in' expressions ':' block else_block?
        asdl_seq* b;
        void *el;
        expr_ty ex;
        void *keyword;
        void *keyword_1;
        void *literal;
        expr_ty t;
        if (
            (keyword = expect_token(p, 517))
            &&
            (t = star_targets_rule(p))
            &&
            (keyword_1 = expect_token(p, 518))
            &&
            (ex = expressions_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (el = else_block_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_For ( t , ex , b , el , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, for_stmt_type, res);
    return res;
}

// with_stmt:
//     | ASYNC 'with' '(' ','.with_item+ ')' ':' block
//     | ASYNC 'with' ','.with_item+ ':' block
//     | 'with' '(' ','.with_item+ ')' ':' block
//     | 'with' ','.with_item+ ':' block
static stmt_ty
with_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, with_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // ASYNC 'with' '(' ','.with_item+ ')' ':' block
        asdl_seq * a;
        void *async_var;
        asdl_seq* b;
        void *keyword;
        void *literal;
        void *literal_1;
        void *literal_2;
        if (
            (async_var = async_token(p))
            &&
            (keyword = expect_token(p, 519))
            &&
            (literal = expect_token(p, 7))
            &&
            (a = _gather_28_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
            &&
            (literal_2 = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_AsyncWith ( a , b , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // ASYNC 'with' ','.with_item+ ':' block
        asdl_seq * a;
        void *async_var;
        asdl_seq* b;
        void *keyword;
        void *literal;
        if (
            (async_var = async_token(p))
            &&
            (keyword = expect_token(p, 519))
            &&
            (a = _gather_30_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_AsyncWith ( a , b , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'with' '(' ','.with_item+ ')' ':' block
        asdl_seq * a;
        asdl_seq* b;
        void *keyword;
        void *literal;
        void *literal_1;
        void *literal_2;
        if (
            (keyword = expect_token(p, 519))
            &&
            (literal = expect_token(p, 7))
            &&
            (a = _gather_32_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
            &&
            (literal_2 = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_With ( a , b , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'with' ','.with_item+ ':' block
        asdl_seq * a;
        asdl_seq* b;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 519))
            &&
            (a = _gather_34_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_With ( a , b , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, with_stmt_type, res);
    return res;
}

// with_item: expression ['as' target]
static withitem_ty
with_item_rule(Parser *p)
{
    withitem_ty res = NULL;
    if (is_memoized(p, with_item_type, &res))
        return res;
    int mark = p->mark;
    { // expression ['as' target]
        expr_ty e;
        void *o;
        if (
            (e = expression_rule(p))
            &&
            (o = _tmp_36_rule(p), 1)
        )
        {
            res = _Py_withitem ( e , o , p -> arena );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, with_item_type, res);
    return res;
}

// try_stmt:
//     | 'try' ':' block finally_block
//     | 'try' ':' block except_block+ else_block? finally_block?
static stmt_ty
try_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, try_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'try' ':' block finally_block
        asdl_seq* b;
        asdl_seq* f;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 511))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (f = finally_block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Try ( b , NULL , NULL , f , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'try' ':' block except_block+ else_block? finally_block?
        asdl_seq* b;
        void *el;
        asdl_seq * ex;
        void *f;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 511))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
            &&
            (ex = _loop1_37_rule(p))
            &&
            (el = else_block_rule(p), 1)
            &&
            (f = finally_block_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Try ( b , ex , el , f , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, try_stmt_type, res);
    return res;
}

// except_block: 'except' expression ['as' target] ':' block | 'except' ':' block
static excepthandler_ty
except_block_rule(Parser *p)
{
    excepthandler_ty res = NULL;
    if (is_memoized(p, except_block_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'except' expression ['as' target] ':' block
        asdl_seq* b;
        expr_ty e;
        void *keyword;
        void *literal;
        void *t;
        if (
            (keyword = expect_token(p, 520))
            &&
            (e = expression_rule(p))
            &&
            (t = _tmp_38_rule(p), 1)
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_ExceptHandler ( e , ( t ) ? ( ( expr_ty ) t ) -> v . Name . id : NULL , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'except' ':' block
        asdl_seq* b;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 520))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_ExceptHandler ( NULL , NULL , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, except_block_type, res);
    return res;
}

// finally_block: 'finally' ':' block
static asdl_seq*
finally_block_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, finally_block_type, &res))
        return res;
    int mark = p->mark;
    { // 'finally' ':' block
        asdl_seq* a;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 521))
            &&
            (literal = expect_token(p, 11))
            &&
            (a = block_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, finally_block_type, res);
    return res;
}

// return_stmt: 'return' expressions?
static stmt_ty
return_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, return_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'return' expressions?
        void *a;
        void *keyword;
        if (
            (keyword = expect_token(p, 500))
            &&
            (a = expressions_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Return ( a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, return_stmt_type, res);
    return res;
}

// raise_stmt: 'raise' expression ['from' expression] | 'raise'
static stmt_ty
raise_stmt_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, raise_stmt_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'raise' expression ['from' expression]
        expr_ty a;
        void *b;
        void *keyword;
        if (
            (keyword = expect_token(p, 501))
            &&
            (a = expression_rule(p))
            &&
            (b = _tmp_39_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Raise ( a , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'raise'
        void *keyword;
        if (
            (keyword = expect_token(p, 501))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Raise ( NULL , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, raise_stmt_type, res);
    return res;
}

// function_def: decorators function_def_raw | function_def_raw
static stmt_ty
function_def_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, function_def_type, &res))
        return res;
    int mark = p->mark;
    { // decorators function_def_raw
        asdl_seq* d;
        stmt_ty f;
        if (
            (d = decorators_rule(p))
            &&
            (f = function_def_raw_rule(p))
        )
        {
            res = function_def_decorators ( p , d , f );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // function_def_raw
        stmt_ty function_def_raw_var;
        if (
            (function_def_raw_var = function_def_raw_rule(p))
        )
        {
            res = function_def_raw_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, function_def_type, res);
    return res;
}

// function_def_raw:
//     | ASYNC 'def' NAME '(' parameters? ')' ['->' annotation] ':' block
//     | 'def' NAME '(' parameters? ')' ['->' annotation] ':' block
static stmt_ty
function_def_raw_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, function_def_raw_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // ASYNC 'def' NAME '(' parameters? ')' ['->' annotation] ':' block
        void *a;
        void *async_var;
        asdl_seq* b;
        void *keyword;
        void *literal;
        void *literal_1;
        void *literal_2;
        expr_ty n;
        void *params;
        if (
            (async_var = async_token(p))
            &&
            (keyword = expect_token(p, 522))
            &&
            (n = name_token(p))
            &&
            (literal = expect_token(p, 7))
            &&
            (params = parameters_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
            &&
            (a = _tmp_40_rule(p), 1)
            &&
            (literal_2 = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_AsyncFunctionDef ( n -> v . Name . id , ( params ) ? params : CHECK ( empty_arguments ( p ) ) , b , NULL , a , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'def' NAME '(' parameters? ')' ['->' annotation] ':' block
        void *a;
        asdl_seq* b;
        void *keyword;
        void *literal;
        void *literal_1;
        void *literal_2;
        expr_ty n;
        void *params;
        if (
            (keyword = expect_token(p, 522))
            &&
            (n = name_token(p))
            &&
            (literal = expect_token(p, 7))
            &&
            (params = parameters_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
            &&
            (a = _tmp_41_rule(p), 1)
            &&
            (literal_2 = expect_token(p, 11))
            &&
            (b = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_FunctionDef ( n -> v . Name . id , ( params ) ? params : CHECK ( empty_arguments ( p ) ) , b , NULL , a , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, function_def_raw_type, res);
    return res;
}

// parameters:
//     | slash_without_default [',' plain_names] [',' names_with_default] [',' star_etc?]
//     | slash_with_default [',' names_with_default] [',' star_etc?]
//     | plain_names [',' names_with_default] [',' star_etc?]
//     | names_with_default [',' star_etc?]
//     | star_etc
static arguments_ty
parameters_rule(Parser *p)
{
    arguments_ty res = NULL;
    if (is_memoized(p, parameters_type, &res))
        return res;
    int mark = p->mark;
    { // slash_without_default [',' plain_names] [',' names_with_default] [',' star_etc?]
        asdl_seq* a;
        void *b;
        void *c;
        void *d;
        if (
            (a = slash_without_default_rule(p))
            &&
            (b = _tmp_42_rule(p), 1)
            &&
            (c = _tmp_43_rule(p), 1)
            &&
            (d = _tmp_44_rule(p), 1)
        )
        {
            res = make_arguments ( p , a , NULL , b , c , d );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // slash_with_default [',' names_with_default] [',' star_etc?]
        SlashWithDefault* a;
        void *b;
        void *c;
        if (
            (a = slash_with_default_rule(p))
            &&
            (b = _tmp_45_rule(p), 1)
            &&
            (c = _tmp_46_rule(p), 1)
        )
        {
            res = make_arguments ( p , NULL , a , NULL , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // plain_names [',' names_with_default] [',' star_etc?]
        asdl_seq* a;
        void *b;
        void *c;
        if (
            (a = plain_names_rule(p))
            &&
            (b = _tmp_47_rule(p), 1)
            &&
            (c = _tmp_48_rule(p), 1)
        )
        {
            res = make_arguments ( p , NULL , NULL , a , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // names_with_default [',' star_etc?]
        asdl_seq* a;
        void *b;
        if (
            (a = names_with_default_rule(p))
            &&
            (b = _tmp_49_rule(p), 1)
        )
        {
            res = make_arguments ( p , NULL , NULL , NULL , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // star_etc
        StarEtc* a;
        if (
            (a = star_etc_rule(p))
        )
        {
            res = make_arguments ( p , NULL , NULL , NULL , NULL , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, parameters_type, res);
    return res;
}

// slash_without_default: plain_names ',' '/'
static asdl_seq*
slash_without_default_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, slash_without_default_type, &res))
        return res;
    int mark = p->mark;
    { // plain_names ',' '/'
        asdl_seq* a;
        void *literal;
        void *literal_1;
        if (
            (a = plain_names_rule(p))
            &&
            (literal = expect_token(p, 12))
            &&
            (literal_1 = expect_token(p, 17))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, slash_without_default_type, res);
    return res;
}

// slash_with_default: [plain_names ','] names_with_default ',' '/'
static SlashWithDefault*
slash_with_default_rule(Parser *p)
{
    SlashWithDefault* res = NULL;
    if (is_memoized(p, slash_with_default_type, &res))
        return res;
    int mark = p->mark;
    { // [plain_names ','] names_with_default ',' '/'
        void *a;
        asdl_seq* b;
        void *literal;
        void *literal_1;
        if (
            (a = _tmp_50_rule(p), 1)
            &&
            (b = names_with_default_rule(p))
            &&
            (literal = expect_token(p, 12))
            &&
            (literal_1 = expect_token(p, 17))
        )
        {
            res = slash_with_default ( p , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, slash_with_default_type, res);
    return res;
}

// star_etc:
//     | '*' plain_name name_with_optional_default* [',' kwds] ','?
//     | '*' name_with_optional_default+ [',' kwds] ','?
//     | kwds ','?
static StarEtc*
star_etc_rule(Parser *p)
{
    StarEtc* res = NULL;
    if (is_memoized(p, star_etc_type, &res))
        return res;
    int mark = p->mark;
    { // '*' plain_name name_with_optional_default* [',' kwds] ','?
        arg_ty a;
        asdl_seq * b;
        void *c;
        void *literal;
        void *opt_var;
        if (
            (literal = expect_token(p, 16))
            &&
            (a = plain_name_rule(p))
            &&
            (b = _loop0_51_rule(p))
            &&
            (c = _tmp_52_rule(p), 1)
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = star_etc ( p , a , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '*' name_with_optional_default+ [',' kwds] ','?
        asdl_seq * b;
        void *c;
        void *literal;
        void *opt_var;
        if (
            (literal = expect_token(p, 16))
            &&
            (b = _loop1_53_rule(p))
            &&
            (c = _tmp_54_rule(p), 1)
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = star_etc ( p , NULL , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // kwds ','?
        arg_ty a;
        void *opt_var;
        if (
            (a = kwds_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = star_etc ( p , NULL , NULL , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_etc_type, res);
    return res;
}

// name_with_optional_default: ',' plain_name ['=' expression]
static NameDefaultPair*
name_with_optional_default_rule(Parser *p)
{
    NameDefaultPair* res = NULL;
    if (is_memoized(p, name_with_optional_default_type, &res))
        return res;
    int mark = p->mark;
    { // ',' plain_name ['=' expression]
        arg_ty a;
        void *b;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (a = plain_name_rule(p))
            &&
            (b = _tmp_55_rule(p), 1)
        )
        {
            res = name_default_pair ( p , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, name_with_optional_default_type, res);
    return res;
}

// names_with_default: ','.name_with_default+
static asdl_seq*
names_with_default_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, names_with_default_type, &res))
        return res;
    int mark = p->mark;
    { // ','.name_with_default+
        asdl_seq * a;
        if (
            (a = _gather_56_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, names_with_default_type, res);
    return res;
}

// name_with_default: plain_name '=' expression
static NameDefaultPair*
name_with_default_rule(Parser *p)
{
    NameDefaultPair* res = NULL;
    if (is_memoized(p, name_with_default_type, &res))
        return res;
    int mark = p->mark;
    { // plain_name '=' expression
        expr_ty e;
        void *literal;
        arg_ty n;
        if (
            (n = plain_name_rule(p))
            &&
            (literal = expect_token(p, 22))
            &&
            (e = expression_rule(p))
        )
        {
            res = name_default_pair ( p , n , e );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, name_with_default_type, res);
    return res;
}

// plain_names: ','.(plain_name !'=')+
static asdl_seq*
plain_names_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, plain_names_type, &res))
        return res;
    int mark = p->mark;
    { // ','.(plain_name !'=')+
        asdl_seq * a;
        if (
            (a = _gather_58_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, plain_names_type, res);
    return res;
}

// plain_name: NAME [':' annotation]
static arg_ty
plain_name_rule(Parser *p)
{
    arg_ty res = NULL;
    if (is_memoized(p, plain_name_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME [':' annotation]
        expr_ty a;
        void *b;
        if (
            (a = name_token(p))
            &&
            (b = _tmp_60_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_arg ( a -> v . Name . id , b , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, plain_name_type, res);
    return res;
}

// kwds: '**' plain_name
static arg_ty
kwds_rule(Parser *p)
{
    arg_ty res = NULL;
    if (is_memoized(p, kwds_type, &res))
        return res;
    int mark = p->mark;
    { // '**' plain_name
        arg_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 35))
            &&
            (a = plain_name_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, kwds_type, res);
    return res;
}

// annotation: expression
static expr_ty
annotation_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, annotation_type, &res))
        return res;
    int mark = p->mark;
    { // expression
        expr_ty expression_var;
        if (
            (expression_var = expression_rule(p))
        )
        {
            res = expression_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, annotation_type, res);
    return res;
}

// decorators: (('@' factor NEWLINE))+
static asdl_seq*
decorators_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, decorators_type, &res))
        return res;
    int mark = p->mark;
    { // (('@' factor NEWLINE))+
        asdl_seq * a;
        if (
            (a = _loop1_61_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, decorators_type, res);
    return res;
}

// class_def: decorators class_def_raw | class_def_raw
static stmt_ty
class_def_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, class_def_type, &res))
        return res;
    int mark = p->mark;
    { // decorators class_def_raw
        asdl_seq* a;
        stmt_ty b;
        if (
            (a = decorators_rule(p))
            &&
            (b = class_def_raw_rule(p))
        )
        {
            res = class_def_decorators ( p , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // class_def_raw
        stmt_ty class_def_raw_var;
        if (
            (class_def_raw_var = class_def_raw_rule(p))
        )
        {
            res = class_def_raw_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, class_def_type, res);
    return res;
}

// class_def_raw: 'class' NAME ['(' arguments? ')'] ':' block
static stmt_ty
class_def_raw_rule(Parser *p)
{
    stmt_ty res = NULL;
    if (is_memoized(p, class_def_raw_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'class' NAME ['(' arguments? ')'] ':' block
        expr_ty a;
        void *b;
        asdl_seq* c;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 523))
            &&
            (a = name_token(p))
            &&
            (b = _tmp_62_rule(p), 1)
            &&
            (literal = expect_token(p, 11))
            &&
            (c = block_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_ClassDef ( a -> v . Name . id , ( b ) ? ( ( expr_ty ) b ) -> v . Call . args : NULL , ( b ) ? ( ( expr_ty ) b ) -> v . Call . keywords : NULL , c , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, class_def_raw_type, res);
    return res;
}

// block: NEWLINE INDENT statements DEDENT | simple_stmt
static asdl_seq*
block_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, block_type, &res))
        return res;
    int mark = p->mark;
    { // NEWLINE INDENT statements DEDENT
        asdl_seq* a;
        void *dedent_var;
        void *indent_var;
        void *newline_var;
        if (
            (newline_var = newline_token(p))
            &&
            (indent_var = indent_token(p))
            &&
            (a = statements_rule(p))
            &&
            (dedent_var = dedent_token(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // simple_stmt
        asdl_seq* simple_stmt_var;
        if (
            (simple_stmt_var = simple_stmt_rule(p))
        )
        {
            res = simple_stmt_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, block_type, res);
    return res;
}

// expressions_list: ','.star_expression+ ','?
static asdl_seq*
expressions_list_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, expressions_list_type, &res))
        return res;
    int mark = p->mark;
    { // ','.star_expression+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_63_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, expressions_list_type, res);
    return res;
}

// expressions:
//     | star_expression ((',' star_expression))+ ','?
//     | star_expression ','
//     | star_expression
static expr_ty
expressions_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, expressions_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // star_expression ((',' star_expression))+ ','?
        expr_ty a;
        asdl_seq * b;
        void *opt_var;
        if (
            (a = star_expression_rule(p))
            &&
            (b = _loop1_65_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( CHECK ( seq_insert_in_front ( p , a , b ) ) , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // star_expression ','
        expr_ty a;
        void *literal;
        if (
            (a = star_expression_rule(p))
            &&
            (literal = expect_token(p, 12))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( CHECK ( singleton_seq ( p , a ) ) , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // star_expression
        expr_ty star_expression_var;
        if (
            (star_expression_var = star_expression_rule(p))
        )
        {
            res = star_expression_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, expressions_type, res);
    return res;
}

// star_expression: '*' bitwise_or | expression
static expr_ty
star_expression_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, star_expression_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '*' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 16))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Starred ( a , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // expression
        expr_ty expression_var;
        if (
            (expression_var = expression_rule(p))
        )
        {
            res = expression_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_expression_type, res);
    return res;
}

// star_named_expressions: ','.star_named_expression+ ','?
static asdl_seq*
star_named_expressions_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, star_named_expressions_type, &res))
        return res;
    int mark = p->mark;
    { // ','.star_named_expression+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_66_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_named_expressions_type, res);
    return res;
}

// star_named_expression: '*' bitwise_or | named_expression
static expr_ty
star_named_expression_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, star_named_expression_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '*' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 16))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Starred ( a , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // named_expression
        expr_ty named_expression_var;
        if (
            (named_expression_var = named_expression_rule(p))
        )
        {
            res = named_expression_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_named_expression_type, res);
    return res;
}

// named_expression: NAME ':=' expression | expression
static expr_ty
named_expression_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, named_expression_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME ':=' expression
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = name_token(p))
            &&
            (literal = expect_token(p, 53))
            &&
            (b = expression_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_NamedExpr ( CHECK ( set_expr_context ( p , a , Store ) ) , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // expression
        expr_ty expression_var;
        if (
            (expression_var = expression_rule(p))
        )
        {
            res = expression_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, named_expression_type, res);
    return res;
}

// annotated_rhs: yield_expr | expressions
static expr_ty
annotated_rhs_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, annotated_rhs_type, &res))
        return res;
    int mark = p->mark;
    { // yield_expr
        expr_ty yield_expr_var;
        if (
            (yield_expr_var = yield_expr_rule(p))
        )
        {
            res = yield_expr_var;
            goto done;
        }
        p->mark = mark;
    }
    { // expressions
        expr_ty expressions_var;
        if (
            (expressions_var = expressions_rule(p))
        )
        {
            res = expressions_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, annotated_rhs_type, res);
    return res;
}

// expression: disjunction 'if' disjunction 'else' expression | disjunction | lambdef
static expr_ty
expression_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, expression_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // disjunction 'if' disjunction 'else' expression
        expr_ty a;
        expr_ty b;
        expr_ty c;
        void *keyword;
        void *keyword_1;
        if (
            (a = disjunction_rule(p))
            &&
            (keyword = expect_token(p, 510))
            &&
            (b = disjunction_rule(p))
            &&
            (keyword_1 = expect_token(p, 516))
            &&
            (c = expression_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_IfExp ( b , a , c , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // disjunction
        expr_ty disjunction_var;
        if (
            (disjunction_var = disjunction_rule(p))
        )
        {
            res = disjunction_var;
            goto done;
        }
        p->mark = mark;
    }
    { // lambdef
        expr_ty lambdef_var;
        if (
            (lambdef_var = lambdef_rule(p))
        )
        {
            res = lambdef_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, expression_type, res);
    return res;
}

// lambdef: 'lambda' lambda_parameters? ':' expression
static expr_ty
lambdef_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, lambdef_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'lambda' lambda_parameters? ':' expression
        void *a;
        expr_ty b;
        void *keyword;
        void *literal;
        if (
            (keyword = expect_token(p, 524))
            &&
            (a = lambda_parameters_rule(p), 1)
            &&
            (literal = expect_token(p, 11))
            &&
            (b = expression_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Lambda ( ( a ) ? a : CHECK ( empty_arguments ( p ) ) , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambdef_type, res);
    return res;
}

// lambda_parameters:
//     | lambda_slash_without_default [',' lambda_plain_names] [',' lambda_names_with_default] [',' lambda_star_etc?]
//     | lambda_slash_with_default [',' lambda_names_with_default] [',' lambda_star_etc?]
//     | lambda_plain_names [',' lambda_names_with_default] [',' lambda_star_etc?]
//     | lambda_names_with_default [',' lambda_star_etc?]
//     | lambda_star_etc
static arguments_ty
lambda_parameters_rule(Parser *p)
{
    arguments_ty res = NULL;
    if (is_memoized(p, lambda_parameters_type, &res))
        return res;
    int mark = p->mark;
    { // lambda_slash_without_default [',' lambda_plain_names] [',' lambda_names_with_default] [',' lambda_star_etc?]
        asdl_seq* a;
        void *b;
        void *c;
        void *d;
        if (
            (a = lambda_slash_without_default_rule(p))
            &&
            (b = _tmp_68_rule(p), 1)
            &&
            (c = _tmp_69_rule(p), 1)
            &&
            (d = _tmp_70_rule(p), 1)
        )
        {
            res = make_arguments ( p , a , NULL , b , c , d );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // lambda_slash_with_default [',' lambda_names_with_default] [',' lambda_star_etc?]
        SlashWithDefault* a;
        void *b;
        void *c;
        if (
            (a = lambda_slash_with_default_rule(p))
            &&
            (b = _tmp_71_rule(p), 1)
            &&
            (c = _tmp_72_rule(p), 1)
        )
        {
            res = make_arguments ( p , NULL , a , NULL , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // lambda_plain_names [',' lambda_names_with_default] [',' lambda_star_etc?]
        asdl_seq* a;
        void *b;
        void *c;
        if (
            (a = lambda_plain_names_rule(p))
            &&
            (b = _tmp_73_rule(p), 1)
            &&
            (c = _tmp_74_rule(p), 1)
        )
        {
            res = make_arguments ( p , NULL , NULL , a , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // lambda_names_with_default [',' lambda_star_etc?]
        asdl_seq* a;
        void *b;
        if (
            (a = lambda_names_with_default_rule(p))
            &&
            (b = _tmp_75_rule(p), 1)
        )
        {
            res = make_arguments ( p , NULL , NULL , NULL , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // lambda_star_etc
        StarEtc* a;
        if (
            (a = lambda_star_etc_rule(p))
        )
        {
            res = make_arguments ( p , NULL , NULL , NULL , NULL , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_parameters_type, res);
    return res;
}

// lambda_slash_without_default: lambda_plain_names ',' '/'
static asdl_seq*
lambda_slash_without_default_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, lambda_slash_without_default_type, &res))
        return res;
    int mark = p->mark;
    { // lambda_plain_names ',' '/'
        asdl_seq* a;
        void *literal;
        void *literal_1;
        if (
            (a = lambda_plain_names_rule(p))
            &&
            (literal = expect_token(p, 12))
            &&
            (literal_1 = expect_token(p, 17))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_slash_without_default_type, res);
    return res;
}

// lambda_slash_with_default: [lambda_plain_names ','] lambda_names_with_default ',' '/'
static SlashWithDefault*
lambda_slash_with_default_rule(Parser *p)
{
    SlashWithDefault* res = NULL;
    if (is_memoized(p, lambda_slash_with_default_type, &res))
        return res;
    int mark = p->mark;
    { // [lambda_plain_names ','] lambda_names_with_default ',' '/'
        void *a;
        asdl_seq* b;
        void *literal;
        void *literal_1;
        if (
            (a = _tmp_76_rule(p), 1)
            &&
            (b = lambda_names_with_default_rule(p))
            &&
            (literal = expect_token(p, 12))
            &&
            (literal_1 = expect_token(p, 17))
        )
        {
            res = slash_with_default ( p , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_slash_with_default_type, res);
    return res;
}

// lambda_star_etc:
//     | '*' lambda_plain_name lambda_name_with_optional_default* [',' lambda_kwds] ','?
//     | '*' lambda_name_with_optional_default+ [',' lambda_kwds] ','?
//     | lambda_kwds ','?
static StarEtc*
lambda_star_etc_rule(Parser *p)
{
    StarEtc* res = NULL;
    if (is_memoized(p, lambda_star_etc_type, &res))
        return res;
    int mark = p->mark;
    { // '*' lambda_plain_name lambda_name_with_optional_default* [',' lambda_kwds] ','?
        arg_ty a;
        asdl_seq * b;
        void *c;
        void *literal;
        void *opt_var;
        if (
            (literal = expect_token(p, 16))
            &&
            (a = lambda_plain_name_rule(p))
            &&
            (b = _loop0_77_rule(p))
            &&
            (c = _tmp_78_rule(p), 1)
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = star_etc ( p , a , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '*' lambda_name_with_optional_default+ [',' lambda_kwds] ','?
        asdl_seq * b;
        void *c;
        void *literal;
        void *opt_var;
        if (
            (literal = expect_token(p, 16))
            &&
            (b = _loop1_79_rule(p))
            &&
            (c = _tmp_80_rule(p), 1)
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = star_etc ( p , NULL , b , c );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // lambda_kwds ','?
        arg_ty a;
        void *opt_var;
        if (
            (a = lambda_kwds_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = star_etc ( p , NULL , NULL , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_star_etc_type, res);
    return res;
}

// lambda_name_with_optional_default: ',' lambda_plain_name ['=' expression]
static NameDefaultPair*
lambda_name_with_optional_default_rule(Parser *p)
{
    NameDefaultPair* res = NULL;
    if (is_memoized(p, lambda_name_with_optional_default_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_plain_name ['=' expression]
        arg_ty a;
        void *b;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (a = lambda_plain_name_rule(p))
            &&
            (b = _tmp_81_rule(p), 1)
        )
        {
            res = name_default_pair ( p , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_name_with_optional_default_type, res);
    return res;
}

// lambda_names_with_default: ','.lambda_name_with_default+
static asdl_seq*
lambda_names_with_default_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, lambda_names_with_default_type, &res))
        return res;
    int mark = p->mark;
    { // ','.lambda_name_with_default+
        asdl_seq * a;
        if (
            (a = _gather_82_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_names_with_default_type, res);
    return res;
}

// lambda_name_with_default: lambda_plain_name '=' expression
static NameDefaultPair*
lambda_name_with_default_rule(Parser *p)
{
    NameDefaultPair* res = NULL;
    if (is_memoized(p, lambda_name_with_default_type, &res))
        return res;
    int mark = p->mark;
    { // lambda_plain_name '=' expression
        expr_ty e;
        void *literal;
        arg_ty n;
        if (
            (n = lambda_plain_name_rule(p))
            &&
            (literal = expect_token(p, 22))
            &&
            (e = expression_rule(p))
        )
        {
            res = name_default_pair ( p , n , e );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_name_with_default_type, res);
    return res;
}

// lambda_plain_names: ','.(lambda_plain_name !'=')+
static asdl_seq*
lambda_plain_names_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, lambda_plain_names_type, &res))
        return res;
    int mark = p->mark;
    { // ','.(lambda_plain_name !'=')+
        asdl_seq * a;
        if (
            (a = _gather_84_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_plain_names_type, res);
    return res;
}

// lambda_plain_name: NAME
static arg_ty
lambda_plain_name_rule(Parser *p)
{
    arg_ty res = NULL;
    if (is_memoized(p, lambda_plain_name_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME
        expr_ty a;
        if (
            (a = name_token(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_arg ( a -> v . Name . id , NULL , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_plain_name_type, res);
    return res;
}

// lambda_kwds: '**' lambda_plain_name
static arg_ty
lambda_kwds_rule(Parser *p)
{
    arg_ty res = NULL;
    if (is_memoized(p, lambda_kwds_type, &res))
        return res;
    int mark = p->mark;
    { // '**' lambda_plain_name
        arg_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 35))
            &&
            (a = lambda_plain_name_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lambda_kwds_type, res);
    return res;
}

// disjunction: conjunction (('or' conjunction))+ | conjunction
static expr_ty
disjunction_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, disjunction_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // conjunction (('or' conjunction))+
        expr_ty a;
        asdl_seq * b;
        if (
            (a = conjunction_rule(p))
            &&
            (b = _loop1_86_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BoolOp ( Or , CHECK ( seq_insert_in_front ( p , a , b ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // conjunction
        expr_ty conjunction_var;
        if (
            (conjunction_var = conjunction_rule(p))
        )
        {
            res = conjunction_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, disjunction_type, res);
    return res;
}

// conjunction: inversion (('and' inversion))+ | inversion
static expr_ty
conjunction_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, conjunction_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // inversion (('and' inversion))+
        expr_ty a;
        asdl_seq * b;
        if (
            (a = inversion_rule(p))
            &&
            (b = _loop1_87_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BoolOp ( And , CHECK ( seq_insert_in_front ( p , a , b ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // inversion
        expr_ty inversion_var;
        if (
            (inversion_var = inversion_rule(p))
        )
        {
            res = inversion_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, conjunction_type, res);
    return res;
}

// inversion: 'not' inversion | comparison
static expr_ty
inversion_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, inversion_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'not' inversion
        expr_ty a;
        void *keyword;
        if (
            (keyword = expect_token(p, 525))
            &&
            (a = inversion_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_UnaryOp ( Not , a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // comparison
        expr_ty comparison_var;
        if (
            (comparison_var = comparison_rule(p))
        )
        {
            res = comparison_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, inversion_type, res);
    return res;
}

// comparison: bitwise_or compare_op_bitwise_or_pair+ | bitwise_or
static expr_ty
comparison_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, comparison_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // bitwise_or compare_op_bitwise_or_pair+
        expr_ty a;
        asdl_seq * b;
        if (
            (a = bitwise_or_rule(p))
            &&
            (b = _loop1_88_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Compare ( a , CHECK ( get_cmpops ( p , b ) ) , CHECK ( get_exprs ( p , b ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // bitwise_or
        expr_ty bitwise_or_var;
        if (
            (bitwise_or_var = bitwise_or_rule(p))
        )
        {
            res = bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, comparison_type, res);
    return res;
}

// compare_op_bitwise_or_pair:
//     | eq_bitwise_or
//     | noteq_bitwise_or
//     | lte_bitwise_or
//     | lt_bitwise_or
//     | gte_bitwise_or
//     | gt_bitwise_or
//     | notin_bitwise_or
//     | in_bitwise_or
//     | isnot_bitwise_or
//     | is_bitwise_or
static CmpopExprPair*
compare_op_bitwise_or_pair_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, compare_op_bitwise_or_pair_type, &res))
        return res;
    int mark = p->mark;
    { // eq_bitwise_or
        CmpopExprPair* eq_bitwise_or_var;
        if (
            (eq_bitwise_or_var = eq_bitwise_or_rule(p))
        )
        {
            res = eq_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // noteq_bitwise_or
        CmpopExprPair* noteq_bitwise_or_var;
        if (
            (noteq_bitwise_or_var = noteq_bitwise_or_rule(p))
        )
        {
            res = noteq_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // lte_bitwise_or
        CmpopExprPair* lte_bitwise_or_var;
        if (
            (lte_bitwise_or_var = lte_bitwise_or_rule(p))
        )
        {
            res = lte_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // lt_bitwise_or
        CmpopExprPair* lt_bitwise_or_var;
        if (
            (lt_bitwise_or_var = lt_bitwise_or_rule(p))
        )
        {
            res = lt_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // gte_bitwise_or
        CmpopExprPair* gte_bitwise_or_var;
        if (
            (gte_bitwise_or_var = gte_bitwise_or_rule(p))
        )
        {
            res = gte_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // gt_bitwise_or
        CmpopExprPair* gt_bitwise_or_var;
        if (
            (gt_bitwise_or_var = gt_bitwise_or_rule(p))
        )
        {
            res = gt_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // notin_bitwise_or
        CmpopExprPair* notin_bitwise_or_var;
        if (
            (notin_bitwise_or_var = notin_bitwise_or_rule(p))
        )
        {
            res = notin_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // in_bitwise_or
        CmpopExprPair* in_bitwise_or_var;
        if (
            (in_bitwise_or_var = in_bitwise_or_rule(p))
        )
        {
            res = in_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // isnot_bitwise_or
        CmpopExprPair* isnot_bitwise_or_var;
        if (
            (isnot_bitwise_or_var = isnot_bitwise_or_rule(p))
        )
        {
            res = isnot_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    { // is_bitwise_or
        CmpopExprPair* is_bitwise_or_var;
        if (
            (is_bitwise_or_var = is_bitwise_or_rule(p))
        )
        {
            res = is_bitwise_or_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, compare_op_bitwise_or_pair_type, res);
    return res;
}

// eq_bitwise_or: '==' bitwise_or
static CmpopExprPair*
eq_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, eq_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // '==' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 27))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , Eq , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, eq_bitwise_or_type, res);
    return res;
}

// noteq_bitwise_or: '!=' bitwise_or
static CmpopExprPair*
noteq_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, noteq_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // '!=' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 28))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , NotEq , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, noteq_bitwise_or_type, res);
    return res;
}

// lte_bitwise_or: '<=' bitwise_or
static CmpopExprPair*
lte_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, lte_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // '<=' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 29))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , LtE , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lte_bitwise_or_type, res);
    return res;
}

// lt_bitwise_or: '<' bitwise_or
static CmpopExprPair*
lt_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, lt_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // '<' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 20))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , Lt , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, lt_bitwise_or_type, res);
    return res;
}

// gte_bitwise_or: '>=' bitwise_or
static CmpopExprPair*
gte_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, gte_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // '>=' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 30))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , GtE , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, gte_bitwise_or_type, res);
    return res;
}

// gt_bitwise_or: '>' bitwise_or
static CmpopExprPair*
gt_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, gt_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // '>' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 21))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , Gt , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, gt_bitwise_or_type, res);
    return res;
}

// notin_bitwise_or: 'not' 'in' bitwise_or
static CmpopExprPair*
notin_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, notin_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // 'not' 'in' bitwise_or
        expr_ty a;
        void *keyword;
        void *keyword_1;
        if (
            (keyword = expect_token(p, 525))
            &&
            (keyword_1 = expect_token(p, 518))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , NotIn , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, notin_bitwise_or_type, res);
    return res;
}

// in_bitwise_or: 'in' bitwise_or
static CmpopExprPair*
in_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, in_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // 'in' bitwise_or
        expr_ty a;
        void *keyword;
        if (
            (keyword = expect_token(p, 518))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , In , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, in_bitwise_or_type, res);
    return res;
}

// isnot_bitwise_or: 'is' 'not' bitwise_or
static CmpopExprPair*
isnot_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, isnot_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // 'is' 'not' bitwise_or
        expr_ty a;
        void *keyword;
        void *keyword_1;
        if (
            (keyword = expect_token(p, 526))
            &&
            (keyword_1 = expect_token(p, 525))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , IsNot , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, isnot_bitwise_or_type, res);
    return res;
}

// is_bitwise_or: 'is' bitwise_or
static CmpopExprPair*
is_bitwise_or_rule(Parser *p)
{
    CmpopExprPair* res = NULL;
    if (is_memoized(p, is_bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    { // 'is' bitwise_or
        expr_ty a;
        void *keyword;
        if (
            (keyword = expect_token(p, 526))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = cmpop_expr_pair ( p , Is , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, is_bitwise_or_type, res);
    return res;
}

// bitwise_or: bitwise_or '|' bitwise_xor | bitwise_xor
static expr_ty bitwise_or_raw(Parser *);
static expr_ty
bitwise_or_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, bitwise_or_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_1 = update_memo(p, mark, bitwise_or_type, res);
        if (tmpvar_1) {
            return res;
        }
        p->mark = mark;
        void *raw = bitwise_or_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
bitwise_or_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // bitwise_or '|' bitwise_xor
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = bitwise_or_rule(p))
            &&
            (literal = expect_token(p, 18))
            &&
            (b = bitwise_xor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , BitOr , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // bitwise_xor
        expr_ty bitwise_xor_var;
        if (
            (bitwise_xor_var = bitwise_xor_rule(p))
        )
        {
            res = bitwise_xor_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// bitwise_xor: bitwise_xor '^' bitwise_and | bitwise_and
static expr_ty bitwise_xor_raw(Parser *);
static expr_ty
bitwise_xor_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, bitwise_xor_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_2 = update_memo(p, mark, bitwise_xor_type, res);
        if (tmpvar_2) {
            return res;
        }
        p->mark = mark;
        void *raw = bitwise_xor_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
bitwise_xor_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // bitwise_xor '^' bitwise_and
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = bitwise_xor_rule(p))
            &&
            (literal = expect_token(p, 32))
            &&
            (b = bitwise_and_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , BitXor , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // bitwise_and
        expr_ty bitwise_and_var;
        if (
            (bitwise_and_var = bitwise_and_rule(p))
        )
        {
            res = bitwise_and_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// bitwise_and: bitwise_and '&' shift_expr | shift_expr
static expr_ty bitwise_and_raw(Parser *);
static expr_ty
bitwise_and_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, bitwise_and_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_3 = update_memo(p, mark, bitwise_and_type, res);
        if (tmpvar_3) {
            return res;
        }
        p->mark = mark;
        void *raw = bitwise_and_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
bitwise_and_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // bitwise_and '&' shift_expr
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = bitwise_and_rule(p))
            &&
            (literal = expect_token(p, 19))
            &&
            (b = shift_expr_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , BitAnd , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // shift_expr
        expr_ty shift_expr_var;
        if (
            (shift_expr_var = shift_expr_rule(p))
        )
        {
            res = shift_expr_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// shift_expr: shift_expr '<<' sum | shift_expr '>>' sum | sum
static expr_ty shift_expr_raw(Parser *);
static expr_ty
shift_expr_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, shift_expr_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_4 = update_memo(p, mark, shift_expr_type, res);
        if (tmpvar_4) {
            return res;
        }
        p->mark = mark;
        void *raw = shift_expr_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
shift_expr_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // shift_expr '<<' sum
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = shift_expr_rule(p))
            &&
            (literal = expect_token(p, 33))
            &&
            (b = sum_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , LShift , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // shift_expr '>>' sum
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = shift_expr_rule(p))
            &&
            (literal = expect_token(p, 34))
            &&
            (b = sum_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , RShift , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // sum
        expr_ty sum_var;
        if (
            (sum_var = sum_rule(p))
        )
        {
            res = sum_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// sum: sum '+' term | sum '-' term | term
static expr_ty sum_raw(Parser *);
static expr_ty
sum_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, sum_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_5 = update_memo(p, mark, sum_type, res);
        if (tmpvar_5) {
            return res;
        }
        p->mark = mark;
        void *raw = sum_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
sum_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // sum '+' term
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = sum_rule(p))
            &&
            (literal = expect_token(p, 14))
            &&
            (b = term_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , Add , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // sum '-' term
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = sum_rule(p))
            &&
            (literal = expect_token(p, 15))
            &&
            (b = term_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , Sub , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // term
        expr_ty term_var;
        if (
            (term_var = term_rule(p))
        )
        {
            res = term_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// term:
//     | term '*' factor
//     | term '/' factor
//     | term '//' factor
//     | term '%' factor
//     | term '@' factor
//     | factor
static expr_ty term_raw(Parser *);
static expr_ty
term_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, term_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_6 = update_memo(p, mark, term_type, res);
        if (tmpvar_6) {
            return res;
        }
        p->mark = mark;
        void *raw = term_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
term_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // term '*' factor
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = term_rule(p))
            &&
            (literal = expect_token(p, 16))
            &&
            (b = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , Mult , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // term '/' factor
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = term_rule(p))
            &&
            (literal = expect_token(p, 17))
            &&
            (b = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , Div , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // term '//' factor
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = term_rule(p))
            &&
            (literal = expect_token(p, 47))
            &&
            (b = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , FloorDiv , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // term '%' factor
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = term_rule(p))
            &&
            (literal = expect_token(p, 24))
            &&
            (b = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , Mod , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // term '@' factor
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = term_rule(p))
            &&
            (literal = expect_token(p, 49))
            &&
            (b = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , MatMult , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // factor
        expr_ty factor_var;
        if (
            (factor_var = factor_rule(p))
        )
        {
            res = factor_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// factor: '+' factor | '-' factor | '~' factor | power
static expr_ty
factor_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, factor_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '+' factor
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 14))
            &&
            (a = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_UnaryOp ( UAdd , a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '-' factor
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 15))
            &&
            (a = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_UnaryOp ( USub , a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '~' factor
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 31))
            &&
            (a = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_UnaryOp ( Invert , a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // power
        expr_ty power_var;
        if (
            (power_var = power_rule(p))
        )
        {
            res = power_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, factor_type, res);
    return res;
}

// power: await_primary '**' factor | await_primary
static expr_ty
power_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, power_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // await_primary '**' factor
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = await_primary_rule(p))
            &&
            (literal = expect_token(p, 35))
            &&
            (b = factor_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_BinOp ( a , Pow , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // await_primary
        expr_ty await_primary_var;
        if (
            (await_primary_var = await_primary_rule(p))
        )
        {
            res = await_primary_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, power_type, res);
    return res;
}

// await_primary: AWAIT primary | primary
static expr_ty
await_primary_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, await_primary_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // AWAIT primary
        expr_ty a;
        void *await_var;
        if (
            (await_var = await_token(p))
            &&
            (a = primary_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Await ( a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // primary
        expr_ty primary_var;
        if (
            (primary_var = primary_rule(p))
        )
        {
            res = primary_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, await_primary_type, res);
    return res;
}

// primary:
//     | primary '.' NAME
//     | primary genexp
//     | primary '(' arguments? ')'
//     | primary '[' slices ']'
//     | atom
static expr_ty primary_raw(Parser *);
static expr_ty
primary_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, primary_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_7 = update_memo(p, mark, primary_type, res);
        if (tmpvar_7) {
            return res;
        }
        p->mark = mark;
        void *raw = primary_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
primary_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // primary '.' NAME
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = primary_rule(p))
            &&
            (literal = expect_token(p, 23))
            &&
            (b = name_token(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Attribute ( a , b -> v . Name . id , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // primary genexp
        expr_ty a;
        expr_ty b;
        if (
            (a = primary_rule(p))
            &&
            (b = genexp_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Call ( a , CHECK ( singleton_seq ( p , b ) ) , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // primary '(' arguments? ')'
        expr_ty a;
        void *b;
        void *literal;
        void *literal_1;
        if (
            (a = primary_rule(p))
            &&
            (literal = expect_token(p, 7))
            &&
            (b = arguments_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Call ( a , ( b ) ? ( ( expr_ty ) b ) -> v . Call . args : NULL , ( b ) ? ( ( expr_ty ) b ) -> v . Call . keywords : NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // primary '[' slices ']'
        expr_ty a;
        expr_ty b;
        void *literal;
        void *literal_1;
        if (
            (a = primary_rule(p))
            &&
            (literal = expect_token(p, 9))
            &&
            (b = slices_rule(p))
            &&
            (literal_1 = expect_token(p, 10))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Subscript ( a , b , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // atom
        expr_ty atom_var;
        if (
            (atom_var = atom_rule(p))
        )
        {
            res = atom_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// slices: slice !',' | ','.slice+ ','?
static expr_ty
slices_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, slices_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // slice !','
        expr_ty a;
        if (
            (a = slice_rule(p))
            &&
            lookahead_with_int(0, expect_token, p, 12)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // ','.slice+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_89_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( a , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, slices_type, res);
    return res;
}

// slice: expression? ':' expression? [':' expression?] | expression
static expr_ty
slice_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, slice_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // expression? ':' expression? [':' expression?]
        void *a;
        void *b;
        void *c;
        void *literal;
        if (
            (a = expression_rule(p), 1)
            &&
            (literal = expect_token(p, 11))
            &&
            (b = expression_rule(p), 1)
            &&
            (c = _tmp_91_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Slice ( a , b , c , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // expression
        expr_ty a;
        if (
            (a = expression_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, slice_type, res);
    return res;
}

// atom:
//     | NAME
//     | 'True'
//     | 'False'
//     | 'None'
//     | &STRING STRING+
//     | NUMBER
//     | &'(' (tuple | group | genexp)
//     | &'[' (list | listcomp)
//     | &'{' (dict | set | dictcomp | setcomp)
//     | '...'
static expr_ty
atom_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, atom_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME
        expr_ty name_var;
        if (
            (name_var = name_token(p))
        )
        {
            res = name_var;
            goto done;
        }
        p->mark = mark;
    }
    { // 'True'
        void *keyword;
        if (
            (keyword = expect_token(p, 527))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Constant ( Py_True , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'False'
        void *keyword;
        if (
            (keyword = expect_token(p, 528))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Constant ( Py_False , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'None'
        void *keyword;
        if (
            (keyword = expect_token(p, 529))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Constant ( Py_None , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // &STRING STRING+
        asdl_seq * a;
        if (
            lookahead(1, string_token, p)
            &&
            (a = _loop1_92_rule(p))
        )
        {
            res = concatenate_strings ( p , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // NUMBER
        expr_ty number_var;
        if (
            (number_var = number_token(p))
        )
        {
            res = number_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'(' (tuple | group | genexp)
        void *_tmp_93_var;
        if (
            lookahead_with_int(1, expect_token, p, 7)
            &&
            (_tmp_93_var = _tmp_93_rule(p))
        )
        {
            res = _tmp_93_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'[' (list | listcomp)
        void *_tmp_94_var;
        if (
            lookahead_with_int(1, expect_token, p, 9)
            &&
            (_tmp_94_var = _tmp_94_rule(p))
        )
        {
            res = _tmp_94_var;
            goto done;
        }
        p->mark = mark;
    }
    { // &'{' (dict | set | dictcomp | setcomp)
        void *_tmp_95_var;
        if (
            lookahead_with_int(1, expect_token, p, 25)
            &&
            (_tmp_95_var = _tmp_95_rule(p))
        )
        {
            res = _tmp_95_var;
            goto done;
        }
        p->mark = mark;
    }
    { // '...'
        void *literal;
        if (
            (literal = expect_token(p, 52))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Constant ( Py_Ellipsis , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, atom_type, res);
    return res;
}

// list: '[' star_named_expressions? ']'
static expr_ty
list_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, list_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '[' star_named_expressions? ']'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 9))
            &&
            (a = star_named_expressions_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 10))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_List ( a , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, list_type, res);
    return res;
}

// listcomp: '[' named_expression for_if_clauses ']'
static expr_ty
listcomp_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, listcomp_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '[' named_expression for_if_clauses ']'
        expr_ty a;
        asdl_seq* b;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 9))
            &&
            (a = named_expression_rule(p))
            &&
            (b = for_if_clauses_rule(p))
            &&
            (literal_1 = expect_token(p, 10))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_ListComp ( a , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, listcomp_type, res);
    return res;
}

// tuple: '(' [star_named_expression ',' star_named_expressions?] ')'
static expr_ty
tuple_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, tuple_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '(' [star_named_expression ',' star_named_expressions?] ')'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = _tmp_96_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( a , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, tuple_type, res);
    return res;
}

// group: '(' (yield_expr | named_expression) ')'
static expr_ty
group_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, group_type, &res))
        return res;
    int mark = p->mark;
    { // '(' (yield_expr | named_expression) ')'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = _tmp_97_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, group_type, res);
    return res;
}

// genexp: '(' expression for_if_clauses ')'
static expr_ty
genexp_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, genexp_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '(' expression for_if_clauses ')'
        expr_ty a;
        asdl_seq* b;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = expression_rule(p))
            &&
            (b = for_if_clauses_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_GeneratorExp ( a , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, genexp_type, res);
    return res;
}

// set: '{' expressions_list '}'
static expr_ty
set_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, set_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '{' expressions_list '}'
        asdl_seq* a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 25))
            &&
            (a = expressions_list_rule(p))
            &&
            (literal_1 = expect_token(p, 26))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Set ( a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, set_type, res);
    return res;
}

// setcomp: '{' expression for_if_clauses '}'
static expr_ty
setcomp_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, setcomp_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '{' expression for_if_clauses '}'
        expr_ty a;
        asdl_seq* b;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 25))
            &&
            (a = expression_rule(p))
            &&
            (b = for_if_clauses_rule(p))
            &&
            (literal_1 = expect_token(p, 26))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_SetComp ( a , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, setcomp_type, res);
    return res;
}

// dict: '{' kvpairs? '}'
static expr_ty
dict_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, dict_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '{' kvpairs? '}'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 25))
            &&
            (a = kvpairs_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 26))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Dict ( CHECK ( get_keys ( p , a ) ) , CHECK ( get_values ( p , a ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, dict_type, res);
    return res;
}

// dictcomp: '{' kvpair for_if_clauses '}'
static expr_ty
dictcomp_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, dictcomp_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '{' kvpair for_if_clauses '}'
        KeyValuePair* a;
        asdl_seq* b;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 25))
            &&
            (a = kvpair_rule(p))
            &&
            (b = for_if_clauses_rule(p))
            &&
            (literal_1 = expect_token(p, 26))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_DictComp ( a -> key , a -> value , b , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, dictcomp_type, res);
    return res;
}

// kvpairs: ','.kvpair+ ','?
static asdl_seq*
kvpairs_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, kvpairs_type, &res))
        return res;
    int mark = p->mark;
    { // ','.kvpair+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_98_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, kvpairs_type, res);
    return res;
}

// kvpair: '**' bitwise_or | expression ':' expression
static KeyValuePair*
kvpair_rule(Parser *p)
{
    KeyValuePair* res = NULL;
    if (is_memoized(p, kvpair_type, &res))
        return res;
    int mark = p->mark;
    { // '**' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 35))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            res = key_value_pair ( p , NULL , a );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // expression ':' expression
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = expression_rule(p))
            &&
            (literal = expect_token(p, 11))
            &&
            (b = expression_rule(p))
        )
        {
            res = key_value_pair ( p , a , b );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, kvpair_type, res);
    return res;
}

// for_if_clauses: ((ASYNC? 'for' star_targets 'in' disjunction (('if' disjunction))*))+
static asdl_seq*
for_if_clauses_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, for_if_clauses_type, &res))
        return res;
    int mark = p->mark;
    { // ((ASYNC? 'for' star_targets 'in' disjunction (('if' disjunction))*))+
        asdl_seq * a;
        if (
            (a = _loop1_100_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, for_if_clauses_type, res);
    return res;
}

// yield_expr: 'yield' 'from' expression | 'yield' expressions?
static expr_ty
yield_expr_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, yield_expr_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // 'yield' 'from' expression
        expr_ty a;
        void *keyword;
        void *keyword_1;
        if (
            (keyword = expect_token(p, 504))
            &&
            (keyword_1 = expect_token(p, 514))
            &&
            (a = expression_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_YieldFrom ( a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // 'yield' expressions?
        void *a;
        void *keyword;
        if (
            (keyword = expect_token(p, 504))
            &&
            (a = expressions_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Yield ( a , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, yield_expr_type, res);
    return res;
}

// arguments: args ','?
static expr_ty
arguments_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, arguments_type, &res))
        return res;
    int mark = p->mark;
    { // args ','?
        expr_ty a;
        void *opt_var;
        if (
            (a = args_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, arguments_type, res);
    return res;
}

// args: starred_expression [',' args] | kwargs | named_expression [',' args]
static expr_ty
args_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, args_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // starred_expression [',' args]
        expr_ty a;
        void *b;
        if (
            (a = starred_expression_rule(p))
            &&
            (b = _tmp_101_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Call ( CONSTRUCTOR ( p ) , ( b ) ? CHECK ( seq_insert_in_front ( p , a , ( ( expr_ty ) b ) -> v . Call . args ) ) : CHECK ( singleton_seq ( p , a ) ) , ( b ) ? ( ( expr_ty ) b ) -> v . Call . keywords : NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // kwargs
        asdl_seq* a;
        if (
            (a = kwargs_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Call ( CONSTRUCTOR ( p ) , CHECK_NULL_ALLOWED ( seq_extract_starred_exprs ( p , a ) ) , CHECK_NULL_ALLOWED ( seq_delete_starred_exprs ( p , a ) ) , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // named_expression [',' args]
        expr_ty a;
        void *b;
        if (
            (a = named_expression_rule(p))
            &&
            (b = _tmp_102_rule(p), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Call ( CONSTRUCTOR ( p ) , ( b ) ? CHECK ( seq_insert_in_front ( p , a , ( ( expr_ty ) b ) -> v . Call . args ) ) : CHECK ( singleton_seq ( p , a ) ) , ( b ) ? ( ( expr_ty ) b ) -> v . Call . keywords : NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, args_type, res);
    return res;
}

// kwargs: ','.kwarg+
static asdl_seq*
kwargs_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, kwargs_type, &res))
        return res;
    int mark = p->mark;
    { // ','.kwarg+
        asdl_seq * a;
        if (
            (a = _gather_103_rule(p))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, kwargs_type, res);
    return res;
}

// starred_expression: '*' expression
static expr_ty
starred_expression_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, starred_expression_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '*' expression
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 16))
            &&
            (a = expression_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Starred ( a , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, starred_expression_type, res);
    return res;
}

// kwarg: NAME '=' expression | starred_expression | '**' expression
static KeywordOrStarred*
kwarg_rule(Parser *p)
{
    KeywordOrStarred* res = NULL;
    if (is_memoized(p, kwarg_type, &res))
        return res;
    int mark = p->mark;
    { // NAME '=' expression
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = name_token(p))
            &&
            (literal = expect_token(p, 22))
            &&
            (b = expression_rule(p))
        )
        {
            res = keyword_or_starred ( p , CHECK ( _Py_keyword ( a -> v . Name . id , b , p -> arena ) ) , 1 );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // starred_expression
        expr_ty a;
        if (
            (a = starred_expression_rule(p))
        )
        {
            res = keyword_or_starred ( p , a , 0 );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '**' expression
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 35))
            &&
            (a = expression_rule(p))
        )
        {
            res = keyword_or_starred ( p , CHECK ( _Py_keyword ( NULL , a , p -> arena ) ) , 1 );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, kwarg_type, res);
    return res;
}

// star_targets: star_target !',' | star_target ((',' star_target))* ','?
static expr_ty
star_targets_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, star_targets_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // star_target !','
        expr_ty a;
        if (
            (a = star_target_rule(p))
            &&
            lookahead_with_int(0, expect_token, p, 12)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // star_target ((',' star_target))* ','?
        expr_ty a;
        asdl_seq * b;
        void *opt_var;
        if (
            (a = star_target_rule(p))
            &&
            (b = _loop0_105_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( CHECK ( seq_insert_in_front ( p , a , b ) ) , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_targets_type, res);
    return res;
}

// star_targets_seq: ','.star_target+ ','?
static asdl_seq*
star_targets_seq_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, star_targets_seq_type, &res))
        return res;
    int mark = p->mark;
    { // ','.star_target+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_106_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_targets_seq_type, res);
    return res;
}

// star_target:
//     | '*' bitwise_or
//     | t_primary '.' NAME !t_lookahead
//     | t_primary '[' slices ']' !t_lookahead
//     | star_atom
static expr_ty
star_target_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, star_target_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // '*' bitwise_or
        expr_ty a;
        void *literal;
        if (
            (literal = expect_token(p, 16))
            &&
            (a = bitwise_or_rule(p))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Starred ( CHECK ( set_expr_context ( p , a , Store ) ) , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary '.' NAME !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 23))
            &&
            (b = name_token(p))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Attribute ( a , b -> v . Name . id , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary '[' slices ']' !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        void *literal_1;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 9))
            &&
            (b = slices_rule(p))
            &&
            (literal_1 = expect_token(p, 10))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Subscript ( a , b , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // star_atom
        expr_ty star_atom_var;
        if (
            (star_atom_var = star_atom_rule(p))
        )
        {
            res = star_atom_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_target_type, res);
    return res;
}

// star_atom:
//     | NAME
//     | '(' star_target ')'
//     | '(' star_targets_seq? ')'
//     | '[' star_targets_seq? ']'
static expr_ty
star_atom_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, star_atom_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME
        expr_ty a;
        if (
            (a = name_token(p))
        )
        {
            res = set_expr_context ( p , a , Store );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '(' star_target ')'
        expr_ty a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = star_target_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = set_expr_context ( p , a , Store );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '(' star_targets_seq? ')'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = star_targets_seq_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( a , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '[' star_targets_seq? ']'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 9))
            &&
            (a = star_targets_seq_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 10))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_List ( a , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, star_atom_type, res);
    return res;
}

// inside_paren_ann_assign_target:
//     | ann_assign_subscript_attribute_target
//     | NAME
//     | '(' inside_paren_ann_assign_target ')'
static expr_ty
inside_paren_ann_assign_target_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, inside_paren_ann_assign_target_type, &res))
        return res;
    int mark = p->mark;
    { // ann_assign_subscript_attribute_target
        expr_ty ann_assign_subscript_attribute_target_var;
        if (
            (ann_assign_subscript_attribute_target_var = ann_assign_subscript_attribute_target_rule(p))
        )
        {
            res = ann_assign_subscript_attribute_target_var;
            goto done;
        }
        p->mark = mark;
    }
    { // NAME
        expr_ty a;
        if (
            (a = name_token(p))
        )
        {
            res = set_expr_context ( p , a , Store );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '(' inside_paren_ann_assign_target ')'
        expr_ty a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = inside_paren_ann_assign_target_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, inside_paren_ann_assign_target_type, res);
    return res;
}

// ann_assign_subscript_attribute_target:
//     | t_primary '.' NAME !t_lookahead
//     | t_primary '[' slices ']' !t_lookahead
static expr_ty
ann_assign_subscript_attribute_target_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, ann_assign_subscript_attribute_target_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // t_primary '.' NAME !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 23))
            &&
            (b = name_token(p))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Attribute ( a , b -> v . Name . id , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary '[' slices ']' !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        void *literal_1;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 9))
            &&
            (b = slices_rule(p))
            &&
            (literal_1 = expect_token(p, 10))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Subscript ( a , b , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, ann_assign_subscript_attribute_target_type, res);
    return res;
}

// del_targets: ','.del_target+ ','?
static asdl_seq*
del_targets_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, del_targets_type, &res))
        return res;
    int mark = p->mark;
    { // ','.del_target+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_108_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, del_targets_type, res);
    return res;
}

// del_target:
//     | t_primary '.' NAME !t_lookahead
//     | t_primary '[' slices ']' !t_lookahead
//     | del_t_atom
static expr_ty
del_target_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, del_target_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // t_primary '.' NAME !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 23))
            &&
            (b = name_token(p))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Attribute ( a , b -> v . Name . id , Del , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary '[' slices ']' !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        void *literal_1;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 9))
            &&
            (b = slices_rule(p))
            &&
            (literal_1 = expect_token(p, 10))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Subscript ( a , b , Del , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // del_t_atom
        expr_ty del_t_atom_var;
        if (
            (del_t_atom_var = del_t_atom_rule(p))
        )
        {
            res = del_t_atom_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, del_target_type, res);
    return res;
}

// del_t_atom: NAME | '(' del_target ')' | '(' del_targets? ')' | '[' del_targets? ']'
static expr_ty
del_t_atom_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, del_t_atom_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME
        expr_ty a;
        if (
            (a = name_token(p))
        )
        {
            res = set_expr_context ( p , a , Del );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '(' del_target ')'
        expr_ty a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = del_target_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = set_expr_context ( p , a , Del );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '(' del_targets? ')'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = del_targets_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( a , Del , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '[' del_targets? ']'
        void *a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 9))
            &&
            (a = del_targets_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 10))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_List ( a , Del , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, del_t_atom_type, res);
    return res;
}

// targets: ','.target+ ','?
static asdl_seq*
targets_rule(Parser *p)
{
    asdl_seq* res = NULL;
    if (is_memoized(p, targets_type, &res))
        return res;
    int mark = p->mark;
    { // ','.target+ ','?
        asdl_seq * a;
        void *opt_var;
        if (
            (a = _gather_110_rule(p))
            &&
            (opt_var = expect_token(p, 12), 1)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, targets_type, res);
    return res;
}

// target:
//     | t_primary '.' NAME !t_lookahead
//     | t_primary '[' slices ']' !t_lookahead
//     | t_atom
static expr_ty
target_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, target_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // t_primary '.' NAME !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 23))
            &&
            (b = name_token(p))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Attribute ( a , b -> v . Name . id , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary '[' slices ']' !t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        void *literal_1;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 9))
            &&
            (b = slices_rule(p))
            &&
            (literal_1 = expect_token(p, 10))
            &&
            lookahead(0, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Subscript ( a , b , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_atom
        expr_ty t_atom_var;
        if (
            (t_atom_var = t_atom_rule(p))
        )
        {
            res = t_atom_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, target_type, res);
    return res;
}

// t_primary:
//     | t_primary '.' NAME &t_lookahead
//     | t_primary '[' slices ']' &t_lookahead
//     | t_primary genexp &t_lookahead
//     | t_primary '(' arguments? ')' &t_lookahead
//     | atom &t_lookahead
static expr_ty t_primary_raw(Parser *);
static expr_ty
t_primary_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, t_primary_type, &res))
        return res;
    int mark = p->mark;
    int resmark = p->mark;
    while (1) {
        int tmpvar_8 = update_memo(p, mark, t_primary_type, res);
        if (tmpvar_8) {
            return res;
        }
        p->mark = mark;
        void *raw = t_primary_raw(p);
        if (raw == NULL || p->mark <= resmark)
            break;
        resmark = p->mark;
        res = raw;
    }
    p->mark = resmark;
    return res;
}
static expr_ty
t_primary_raw(Parser *p)
{
    expr_ty res = NULL;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // t_primary '.' NAME &t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 23))
            &&
            (b = name_token(p))
            &&
            lookahead(1, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Attribute ( a , b -> v . Name . id , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary '[' slices ']' &t_lookahead
        expr_ty a;
        expr_ty b;
        void *literal;
        void *literal_1;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 9))
            &&
            (b = slices_rule(p))
            &&
            (literal_1 = expect_token(p, 10))
            &&
            lookahead(1, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Subscript ( a , b , Load , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary genexp &t_lookahead
        expr_ty a;
        expr_ty b;
        if (
            (a = t_primary_rule(p))
            &&
            (b = genexp_rule(p))
            &&
            lookahead(1, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Call ( a , CHECK ( singleton_seq ( p , b ) ) , NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // t_primary '(' arguments? ')' &t_lookahead
        expr_ty a;
        void *b;
        void *literal;
        void *literal_1;
        if (
            (a = t_primary_rule(p))
            &&
            (literal = expect_token(p, 7))
            &&
            (b = arguments_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
            &&
            lookahead(1, t_lookahead_rule, p)
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Call ( a , ( b ) ? ( ( expr_ty ) b ) -> v . Call . args : NULL , ( b ) ? ( ( expr_ty ) b ) -> v . Call . keywords : NULL , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // atom &t_lookahead
        expr_ty a;
        if (
            (a = atom_rule(p))
            &&
            lookahead(1, t_lookahead_rule, p)
        )
        {
            res = a;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    return res;
}

// t_lookahead: '(' | '[' | '.'
static void *
t_lookahead_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, t_lookahead_type, &res))
        return res;
    int mark = p->mark;
    { // '('
        void *literal;
        if (
            (literal = expect_token(p, 7))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    { // '['
        void *literal;
        if (
            (literal = expect_token(p, 9))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    { // '.'
        void *literal;
        if (
            (literal = expect_token(p, 23))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, t_lookahead_type, res);
    return res;
}

// t_atom: NAME | '(' target ')' | '(' targets? ')' | '[' targets? ']'
static expr_ty
t_atom_rule(Parser *p)
{
    expr_ty res = NULL;
    if (is_memoized(p, t_atom_type, &res))
        return res;
    int mark = p->mark;
    if (p->mark == p->fill && fill_token(p) < 0) {
        return NULL;
    }
    int start_lineno = p->tokens[mark]->lineno;
    UNUSED(start_lineno); // Only used by EXTRA macro
    int start_col_offset = p->tokens[mark]->col_offset;
    UNUSED(start_col_offset); // Only used by EXTRA macro
    { // NAME
        expr_ty a;
        if (
            (a = name_token(p))
        )
        {
            res = set_expr_context ( p , a , Store );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '(' target ')'
        expr_ty a;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (a = target_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = set_expr_context ( p , a , Store );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '(' targets? ')'
        void *b;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (b = targets_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_Tuple ( b , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // '[' targets? ']'
        void *b;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 9))
            &&
            (b = targets_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 10))
        )
        {
            Token *token = get_last_nonnwhitespace_token(p);
            if (token == NULL) {
                return NULL;
            }
            int end_lineno = token->end_lineno;
            UNUSED(end_lineno); // Only used by EXTRA macro
            int end_col_offset = token->end_col_offset;
            UNUSED(end_col_offset); // Only used by EXTRA macro
            res = _Py_List ( b , Store , EXTRA );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, t_atom_type, res);
    return res;
}

// _loop1_1: statement
static asdl_seq *
_loop1_1_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_1_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // statement
        asdl_seq* statement_var;
        while (
            (statement_var = statement_rule(p))
        )
        {
            res = statement_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_1");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_1");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_1_type, seq);
    return seq;
}

// _loop0_3: ';' small_stmt
static asdl_seq *
_loop0_3_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_3_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ';' small_stmt
        stmt_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 13))
            &&
            (elem = small_stmt_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_3");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_3");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_3_type, seq);
    return seq;
}

// _gather_2: small_stmt _loop0_3
static asdl_seq *
_gather_2_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_2_type, &res))
        return res;
    int mark = p->mark;
    { // small_stmt _loop0_3
        stmt_ty elem;
        asdl_seq * seq;
        if (
            (elem = small_stmt_rule(p))
            &&
            (seq = _loop0_3_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_2_type, res);
    return res;
}

// _tmp_4: 'import' | 'from'
static void *
_tmp_4_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_4_type, &res))
        return res;
    int mark = p->mark;
    { // 'import'
        void *keyword;
        if (
            (keyword = expect_token(p, 513))
        )
        {
            res = keyword;
            goto done;
        }
        p->mark = mark;
    }
    { // 'from'
        void *keyword;
        if (
            (keyword = expect_token(p, 514))
        )
        {
            res = keyword;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_4_type, res);
    return res;
}

// _tmp_5: 'def' | '@' | ASYNC
static void *
_tmp_5_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_5_type, &res))
        return res;
    int mark = p->mark;
    { // 'def'
        void *keyword;
        if (
            (keyword = expect_token(p, 522))
        )
        {
            res = keyword;
            goto done;
        }
        p->mark = mark;
    }
    { // '@'
        void *literal;
        if (
            (literal = expect_token(p, 49))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    { // ASYNC
        void *async_var;
        if (
            (async_var = async_token(p))
        )
        {
            res = async_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_5_type, res);
    return res;
}

// _tmp_6: 'class' | '@'
static void *
_tmp_6_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_6_type, &res))
        return res;
    int mark = p->mark;
    { // 'class'
        void *keyword;
        if (
            (keyword = expect_token(p, 523))
        )
        {
            res = keyword;
            goto done;
        }
        p->mark = mark;
    }
    { // '@'
        void *literal;
        if (
            (literal = expect_token(p, 49))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_6_type, res);
    return res;
}

// _tmp_7: 'with' | ASYNC
static void *
_tmp_7_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_7_type, &res))
        return res;
    int mark = p->mark;
    { // 'with'
        void *keyword;
        if (
            (keyword = expect_token(p, 519))
        )
        {
            res = keyword;
            goto done;
        }
        p->mark = mark;
    }
    { // ASYNC
        void *async_var;
        if (
            (async_var = async_token(p))
        )
        {
            res = async_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_7_type, res);
    return res;
}

// _tmp_8: 'for' | ASYNC
static void *
_tmp_8_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_8_type, &res))
        return res;
    int mark = p->mark;
    { // 'for'
        void *keyword;
        if (
            (keyword = expect_token(p, 517))
        )
        {
            res = keyword;
            goto done;
        }
        p->mark = mark;
    }
    { // ASYNC
        void *async_var;
        if (
            (async_var = async_token(p))
        )
        {
            res = async_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_8_type, res);
    return res;
}

// _tmp_9: '=' annotated_rhs
static void *
_tmp_9_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_9_type, &res))
        return res;
    int mark = p->mark;
    { // '=' annotated_rhs
        expr_ty d;
        void *literal;
        if (
            (literal = expect_token(p, 22))
            &&
            (d = annotated_rhs_rule(p))
        )
        {
            res = d;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_9_type, res);
    return res;
}

// _tmp_10: '(' inside_paren_ann_assign_target ')' | ann_assign_subscript_attribute_target
static void *
_tmp_10_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_10_type, &res))
        return res;
    int mark = p->mark;
    { // '(' inside_paren_ann_assign_target ')'
        expr_ty b;
        void *literal;
        void *literal_1;
        if (
            (literal = expect_token(p, 7))
            &&
            (b = inside_paren_ann_assign_target_rule(p))
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = b;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    { // ann_assign_subscript_attribute_target
        expr_ty ann_assign_subscript_attribute_target_var;
        if (
            (ann_assign_subscript_attribute_target_var = ann_assign_subscript_attribute_target_rule(p))
        )
        {
            res = ann_assign_subscript_attribute_target_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_10_type, res);
    return res;
}

// _tmp_11: '=' annotated_rhs
static void *
_tmp_11_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_11_type, &res))
        return res;
    int mark = p->mark;
    { // '=' annotated_rhs
        expr_ty d;
        void *literal;
        if (
            (literal = expect_token(p, 22))
            &&
            (d = annotated_rhs_rule(p))
        )
        {
            res = d;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_11_type, res);
    return res;
}

// _loop1_12: (star_targets '=')
static asdl_seq *
_loop1_12_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_12_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // (star_targets '=')
        void *_tmp_112_var;
        while (
            (_tmp_112_var = _tmp_112_rule(p))
        )
        {
            res = _tmp_112_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_12");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_12");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_12_type, seq);
    return seq;
}

// _tmp_13: yield_expr | expressions
static void *
_tmp_13_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_13_type, &res))
        return res;
    int mark = p->mark;
    { // yield_expr
        expr_ty yield_expr_var;
        if (
            (yield_expr_var = yield_expr_rule(p))
        )
        {
            res = yield_expr_var;
            goto done;
        }
        p->mark = mark;
    }
    { // expressions
        expr_ty expressions_var;
        if (
            (expressions_var = expressions_rule(p))
        )
        {
            res = expressions_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_13_type, res);
    return res;
}

// _tmp_14: yield_expr | expressions
static void *
_tmp_14_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_14_type, &res))
        return res;
    int mark = p->mark;
    { // yield_expr
        expr_ty yield_expr_var;
        if (
            (yield_expr_var = yield_expr_rule(p))
        )
        {
            res = yield_expr_var;
            goto done;
        }
        p->mark = mark;
    }
    { // expressions
        expr_ty expressions_var;
        if (
            (expressions_var = expressions_rule(p))
        )
        {
            res = expressions_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_14_type, res);
    return res;
}

// _loop0_16: ',' NAME
static asdl_seq *
_loop0_16_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_16_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' NAME
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = name_token(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_16");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_16");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_16_type, seq);
    return seq;
}

// _gather_15: NAME _loop0_16
static asdl_seq *
_gather_15_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_15_type, &res))
        return res;
    int mark = p->mark;
    { // NAME _loop0_16
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = name_token(p))
            &&
            (seq = _loop0_16_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_15_type, res);
    return res;
}

// _loop0_18: ',' NAME
static asdl_seq *
_loop0_18_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_18_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' NAME
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = name_token(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_18");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_18");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_18_type, seq);
    return seq;
}

// _gather_17: NAME _loop0_18
static asdl_seq *
_gather_17_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_17_type, &res))
        return res;
    int mark = p->mark;
    { // NAME _loop0_18
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = name_token(p))
            &&
            (seq = _loop0_18_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_17_type, res);
    return res;
}

// _tmp_19: ',' expression
static void *
_tmp_19_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_19_type, &res))
        return res;
    int mark = p->mark;
    { // ',' expression
        void *literal;
        expr_ty z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = expression_rule(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_19_type, res);
    return res;
}

// _loop0_20: ('.' | '...')
static asdl_seq *
_loop0_20_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_20_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ('.' | '...')
        void *_tmp_113_var;
        while (
            (_tmp_113_var = _tmp_113_rule(p))
        )
        {
            res = _tmp_113_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_20");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_20");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_20_type, seq);
    return seq;
}

// _loop1_21: ('.' | '...')
static asdl_seq *
_loop1_21_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_21_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ('.' | '...')
        void *_tmp_114_var;
        while (
            (_tmp_114_var = _tmp_114_rule(p))
        )
        {
            res = _tmp_114_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_21");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_21");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_21_type, seq);
    return seq;
}

// _loop0_23: ',' import_from_as_name
static asdl_seq *
_loop0_23_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_23_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' import_from_as_name
        alias_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = import_from_as_name_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_23");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_23");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_23_type, seq);
    return seq;
}

// _gather_22: import_from_as_name _loop0_23
static asdl_seq *
_gather_22_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_22_type, &res))
        return res;
    int mark = p->mark;
    { // import_from_as_name _loop0_23
        alias_ty elem;
        asdl_seq * seq;
        if (
            (elem = import_from_as_name_rule(p))
            &&
            (seq = _loop0_23_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_22_type, res);
    return res;
}

// _tmp_24: 'as' NAME
static void *
_tmp_24_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_24_type, &res))
        return res;
    int mark = p->mark;
    { // 'as' NAME
        void *keyword;
        expr_ty z;
        if (
            (keyword = expect_token(p, 530))
            &&
            (z = name_token(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_24_type, res);
    return res;
}

// _loop0_26: ',' dotted_as_name
static asdl_seq *
_loop0_26_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_26_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' dotted_as_name
        alias_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = dotted_as_name_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_26");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_26");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_26_type, seq);
    return seq;
}

// _gather_25: dotted_as_name _loop0_26
static asdl_seq *
_gather_25_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_25_type, &res))
        return res;
    int mark = p->mark;
    { // dotted_as_name _loop0_26
        alias_ty elem;
        asdl_seq * seq;
        if (
            (elem = dotted_as_name_rule(p))
            &&
            (seq = _loop0_26_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_25_type, res);
    return res;
}

// _tmp_27: 'as' NAME
static void *
_tmp_27_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_27_type, &res))
        return res;
    int mark = p->mark;
    { // 'as' NAME
        void *keyword;
        expr_ty z;
        if (
            (keyword = expect_token(p, 530))
            &&
            (z = name_token(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_27_type, res);
    return res;
}

// _loop0_29: ',' with_item
static asdl_seq *
_loop0_29_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_29_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' with_item
        withitem_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = with_item_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_29");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_29");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_29_type, seq);
    return seq;
}

// _gather_28: with_item _loop0_29
static asdl_seq *
_gather_28_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_28_type, &res))
        return res;
    int mark = p->mark;
    { // with_item _loop0_29
        withitem_ty elem;
        asdl_seq * seq;
        if (
            (elem = with_item_rule(p))
            &&
            (seq = _loop0_29_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_28_type, res);
    return res;
}

// _loop0_31: ',' with_item
static asdl_seq *
_loop0_31_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_31_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' with_item
        withitem_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = with_item_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_31");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_31");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_31_type, seq);
    return seq;
}

// _gather_30: with_item _loop0_31
static asdl_seq *
_gather_30_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_30_type, &res))
        return res;
    int mark = p->mark;
    { // with_item _loop0_31
        withitem_ty elem;
        asdl_seq * seq;
        if (
            (elem = with_item_rule(p))
            &&
            (seq = _loop0_31_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_30_type, res);
    return res;
}

// _loop0_33: ',' with_item
static asdl_seq *
_loop0_33_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_33_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' with_item
        withitem_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = with_item_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_33");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_33");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_33_type, seq);
    return seq;
}

// _gather_32: with_item _loop0_33
static asdl_seq *
_gather_32_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_32_type, &res))
        return res;
    int mark = p->mark;
    { // with_item _loop0_33
        withitem_ty elem;
        asdl_seq * seq;
        if (
            (elem = with_item_rule(p))
            &&
            (seq = _loop0_33_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_32_type, res);
    return res;
}

// _loop0_35: ',' with_item
static asdl_seq *
_loop0_35_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_35_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' with_item
        withitem_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = with_item_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_35");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_35");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_35_type, seq);
    return seq;
}

// _gather_34: with_item _loop0_35
static asdl_seq *
_gather_34_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_34_type, &res))
        return res;
    int mark = p->mark;
    { // with_item _loop0_35
        withitem_ty elem;
        asdl_seq * seq;
        if (
            (elem = with_item_rule(p))
            &&
            (seq = _loop0_35_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_34_type, res);
    return res;
}

// _tmp_36: 'as' target
static void *
_tmp_36_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_36_type, &res))
        return res;
    int mark = p->mark;
    { // 'as' target
        void *keyword;
        expr_ty t;
        if (
            (keyword = expect_token(p, 530))
            &&
            (t = target_rule(p))
        )
        {
            res = t;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_36_type, res);
    return res;
}

// _loop1_37: except_block
static asdl_seq *
_loop1_37_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_37_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // except_block
        excepthandler_ty except_block_var;
        while (
            (except_block_var = except_block_rule(p))
        )
        {
            res = except_block_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_37");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_37");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_37_type, seq);
    return seq;
}

// _tmp_38: 'as' target
static void *
_tmp_38_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_38_type, &res))
        return res;
    int mark = p->mark;
    { // 'as' target
        void *keyword;
        expr_ty z;
        if (
            (keyword = expect_token(p, 530))
            &&
            (z = target_rule(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_38_type, res);
    return res;
}

// _tmp_39: 'from' expression
static void *
_tmp_39_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_39_type, &res))
        return res;
    int mark = p->mark;
    { // 'from' expression
        void *keyword;
        expr_ty z;
        if (
            (keyword = expect_token(p, 514))
            &&
            (z = expression_rule(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_39_type, res);
    return res;
}

// _tmp_40: '->' annotation
static void *
_tmp_40_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_40_type, &res))
        return res;
    int mark = p->mark;
    { // '->' annotation
        void *literal;
        expr_ty z;
        if (
            (literal = expect_token(p, 51))
            &&
            (z = annotation_rule(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_40_type, res);
    return res;
}

// _tmp_41: '->' annotation
static void *
_tmp_41_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_41_type, &res))
        return res;
    int mark = p->mark;
    { // '->' annotation
        void *literal;
        expr_ty z;
        if (
            (literal = expect_token(p, 51))
            &&
            (z = annotation_rule(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_41_type, res);
    return res;
}

// _tmp_42: ',' plain_names
static void *
_tmp_42_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_42_type, &res))
        return res;
    int mark = p->mark;
    { // ',' plain_names
        void *literal;
        asdl_seq* x;
        if (
            (literal = expect_token(p, 12))
            &&
            (x = plain_names_rule(p))
        )
        {
            res = x;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_42_type, res);
    return res;
}

// _tmp_43: ',' names_with_default
static void *
_tmp_43_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_43_type, &res))
        return res;
    int mark = p->mark;
    { // ',' names_with_default
        void *literal;
        asdl_seq* y;
        if (
            (literal = expect_token(p, 12))
            &&
            (y = names_with_default_rule(p))
        )
        {
            res = y;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_43_type, res);
    return res;
}

// _tmp_44: ',' star_etc?
static void *
_tmp_44_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_44_type, &res))
        return res;
    int mark = p->mark;
    { // ',' star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_44_type, res);
    return res;
}

// _tmp_45: ',' names_with_default
static void *
_tmp_45_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_45_type, &res))
        return res;
    int mark = p->mark;
    { // ',' names_with_default
        void *literal;
        asdl_seq* y;
        if (
            (literal = expect_token(p, 12))
            &&
            (y = names_with_default_rule(p))
        )
        {
            res = y;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_45_type, res);
    return res;
}

// _tmp_46: ',' star_etc?
static void *
_tmp_46_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_46_type, &res))
        return res;
    int mark = p->mark;
    { // ',' star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_46_type, res);
    return res;
}

// _tmp_47: ',' names_with_default
static void *
_tmp_47_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_47_type, &res))
        return res;
    int mark = p->mark;
    { // ',' names_with_default
        void *literal;
        asdl_seq* y;
        if (
            (literal = expect_token(p, 12))
            &&
            (y = names_with_default_rule(p))
        )
        {
            res = y;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_47_type, res);
    return res;
}

// _tmp_48: ',' star_etc?
static void *
_tmp_48_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_48_type, &res))
        return res;
    int mark = p->mark;
    { // ',' star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_48_type, res);
    return res;
}

// _tmp_49: ',' star_etc?
static void *
_tmp_49_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_49_type, &res))
        return res;
    int mark = p->mark;
    { // ',' star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_49_type, res);
    return res;
}

// _tmp_50: plain_names ','
static void *
_tmp_50_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_50_type, &res))
        return res;
    int mark = p->mark;
    { // plain_names ','
        void *literal;
        asdl_seq* n;
        if (
            (n = plain_names_rule(p))
            &&
            (literal = expect_token(p, 12))
        )
        {
            res = n;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_50_type, res);
    return res;
}

// _loop0_51: name_with_optional_default
static asdl_seq *
_loop0_51_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_51_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // name_with_optional_default
        NameDefaultPair* name_with_optional_default_var;
        while (
            (name_with_optional_default_var = name_with_optional_default_rule(p))
        )
        {
            res = name_with_optional_default_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_51");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_51");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_51_type, seq);
    return seq;
}

// _tmp_52: ',' kwds
static void *
_tmp_52_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_52_type, &res))
        return res;
    int mark = p->mark;
    { // ',' kwds
        arg_ty d;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (d = kwds_rule(p))
        )
        {
            res = d;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_52_type, res);
    return res;
}

// _loop1_53: name_with_optional_default
static asdl_seq *
_loop1_53_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_53_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // name_with_optional_default
        NameDefaultPair* name_with_optional_default_var;
        while (
            (name_with_optional_default_var = name_with_optional_default_rule(p))
        )
        {
            res = name_with_optional_default_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_53");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_53");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_53_type, seq);
    return seq;
}

// _tmp_54: ',' kwds
static void *
_tmp_54_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_54_type, &res))
        return res;
    int mark = p->mark;
    { // ',' kwds
        arg_ty d;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (d = kwds_rule(p))
        )
        {
            res = d;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_54_type, res);
    return res;
}

// _tmp_55: '=' expression
static void *
_tmp_55_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_55_type, &res))
        return res;
    int mark = p->mark;
    { // '=' expression
        expr_ty e;
        void *literal;
        if (
            (literal = expect_token(p, 22))
            &&
            (e = expression_rule(p))
        )
        {
            res = e;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_55_type, res);
    return res;
}

// _loop0_57: ',' name_with_default
static asdl_seq *
_loop0_57_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_57_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' name_with_default
        NameDefaultPair* elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = name_with_default_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_57");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_57");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_57_type, seq);
    return seq;
}

// _gather_56: name_with_default _loop0_57
static asdl_seq *
_gather_56_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_56_type, &res))
        return res;
    int mark = p->mark;
    { // name_with_default _loop0_57
        NameDefaultPair* elem;
        asdl_seq * seq;
        if (
            (elem = name_with_default_rule(p))
            &&
            (seq = _loop0_57_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_56_type, res);
    return res;
}

// _loop0_59: ',' (plain_name !'=')
static asdl_seq *
_loop0_59_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_59_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' (plain_name !'=')
        void *elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = _tmp_115_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_59");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_59");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_59_type, seq);
    return seq;
}

// _gather_58: (plain_name !'=') _loop0_59
static asdl_seq *
_gather_58_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_58_type, &res))
        return res;
    int mark = p->mark;
    { // (plain_name !'=') _loop0_59
        void *elem;
        asdl_seq * seq;
        if (
            (elem = _tmp_115_rule(p))
            &&
            (seq = _loop0_59_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_58_type, res);
    return res;
}

// _tmp_60: ':' annotation
static void *
_tmp_60_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_60_type, &res))
        return res;
    int mark = p->mark;
    { // ':' annotation
        void *literal;
        expr_ty z;
        if (
            (literal = expect_token(p, 11))
            &&
            (z = annotation_rule(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_60_type, res);
    return res;
}

// _loop1_61: ('@' factor NEWLINE)
static asdl_seq *
_loop1_61_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_61_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ('@' factor NEWLINE)
        void *_tmp_116_var;
        while (
            (_tmp_116_var = _tmp_116_rule(p))
        )
        {
            res = _tmp_116_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_61");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_61");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_61_type, seq);
    return seq;
}

// _tmp_62: '(' arguments? ')'
static void *
_tmp_62_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_62_type, &res))
        return res;
    int mark = p->mark;
    { // '(' arguments? ')'
        void *literal;
        void *literal_1;
        void *z;
        if (
            (literal = expect_token(p, 7))
            &&
            (z = arguments_rule(p), 1)
            &&
            (literal_1 = expect_token(p, 8))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_62_type, res);
    return res;
}

// _loop0_64: ',' star_expression
static asdl_seq *
_loop0_64_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_64_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' star_expression
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = star_expression_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_64");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_64");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_64_type, seq);
    return seq;
}

// _gather_63: star_expression _loop0_64
static asdl_seq *
_gather_63_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_63_type, &res))
        return res;
    int mark = p->mark;
    { // star_expression _loop0_64
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = star_expression_rule(p))
            &&
            (seq = _loop0_64_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_63_type, res);
    return res;
}

// _loop1_65: (',' star_expression)
static asdl_seq *
_loop1_65_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_65_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // (',' star_expression)
        void *_tmp_117_var;
        while (
            (_tmp_117_var = _tmp_117_rule(p))
        )
        {
            res = _tmp_117_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_65");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_65");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_65_type, seq);
    return seq;
}

// _loop0_67: ',' star_named_expression
static asdl_seq *
_loop0_67_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_67_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' star_named_expression
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = star_named_expression_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_67");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_67");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_67_type, seq);
    return seq;
}

// _gather_66: star_named_expression _loop0_67
static asdl_seq *
_gather_66_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_66_type, &res))
        return res;
    int mark = p->mark;
    { // star_named_expression _loop0_67
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = star_named_expression_rule(p))
            &&
            (seq = _loop0_67_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_66_type, res);
    return res;
}

// _tmp_68: ',' lambda_plain_names
static void *
_tmp_68_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_68_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_plain_names
        void *literal;
        asdl_seq* x;
        if (
            (literal = expect_token(p, 12))
            &&
            (x = lambda_plain_names_rule(p))
        )
        {
            res = x;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_68_type, res);
    return res;
}

// _tmp_69: ',' lambda_names_with_default
static void *
_tmp_69_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_69_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_names_with_default
        void *literal;
        asdl_seq* y;
        if (
            (literal = expect_token(p, 12))
            &&
            (y = lambda_names_with_default_rule(p))
        )
        {
            res = y;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_69_type, res);
    return res;
}

// _tmp_70: ',' lambda_star_etc?
static void *
_tmp_70_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_70_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = lambda_star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_70_type, res);
    return res;
}

// _tmp_71: ',' lambda_names_with_default
static void *
_tmp_71_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_71_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_names_with_default
        void *literal;
        asdl_seq* y;
        if (
            (literal = expect_token(p, 12))
            &&
            (y = lambda_names_with_default_rule(p))
        )
        {
            res = y;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_71_type, res);
    return res;
}

// _tmp_72: ',' lambda_star_etc?
static void *
_tmp_72_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_72_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = lambda_star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_72_type, res);
    return res;
}

// _tmp_73: ',' lambda_names_with_default
static void *
_tmp_73_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_73_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_names_with_default
        void *literal;
        asdl_seq* y;
        if (
            (literal = expect_token(p, 12))
            &&
            (y = lambda_names_with_default_rule(p))
        )
        {
            res = y;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_73_type, res);
    return res;
}

// _tmp_74: ',' lambda_star_etc?
static void *
_tmp_74_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_74_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = lambda_star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_74_type, res);
    return res;
}

// _tmp_75: ',' lambda_star_etc?
static void *
_tmp_75_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_75_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_star_etc?
        void *literal;
        void *z;
        if (
            (literal = expect_token(p, 12))
            &&
            (z = lambda_star_etc_rule(p), 1)
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_75_type, res);
    return res;
}

// _tmp_76: lambda_plain_names ','
static void *
_tmp_76_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_76_type, &res))
        return res;
    int mark = p->mark;
    { // lambda_plain_names ','
        void *literal;
        asdl_seq* n;
        if (
            (n = lambda_plain_names_rule(p))
            &&
            (literal = expect_token(p, 12))
        )
        {
            res = n;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_76_type, res);
    return res;
}

// _loop0_77: lambda_name_with_optional_default
static asdl_seq *
_loop0_77_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_77_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // lambda_name_with_optional_default
        NameDefaultPair* lambda_name_with_optional_default_var;
        while (
            (lambda_name_with_optional_default_var = lambda_name_with_optional_default_rule(p))
        )
        {
            res = lambda_name_with_optional_default_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_77");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_77");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_77_type, seq);
    return seq;
}

// _tmp_78: ',' lambda_kwds
static void *
_tmp_78_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_78_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_kwds
        arg_ty d;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (d = lambda_kwds_rule(p))
        )
        {
            res = d;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_78_type, res);
    return res;
}

// _loop1_79: lambda_name_with_optional_default
static asdl_seq *
_loop1_79_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_79_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // lambda_name_with_optional_default
        NameDefaultPair* lambda_name_with_optional_default_var;
        while (
            (lambda_name_with_optional_default_var = lambda_name_with_optional_default_rule(p))
        )
        {
            res = lambda_name_with_optional_default_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_79");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_79");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_79_type, seq);
    return seq;
}

// _tmp_80: ',' lambda_kwds
static void *
_tmp_80_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_80_type, &res))
        return res;
    int mark = p->mark;
    { // ',' lambda_kwds
        arg_ty d;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (d = lambda_kwds_rule(p))
        )
        {
            res = d;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_80_type, res);
    return res;
}

// _tmp_81: '=' expression
static void *
_tmp_81_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_81_type, &res))
        return res;
    int mark = p->mark;
    { // '=' expression
        expr_ty e;
        void *literal;
        if (
            (literal = expect_token(p, 22))
            &&
            (e = expression_rule(p))
        )
        {
            res = e;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_81_type, res);
    return res;
}

// _loop0_83: ',' lambda_name_with_default
static asdl_seq *
_loop0_83_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_83_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' lambda_name_with_default
        NameDefaultPair* elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = lambda_name_with_default_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_83");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_83");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_83_type, seq);
    return seq;
}

// _gather_82: lambda_name_with_default _loop0_83
static asdl_seq *
_gather_82_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_82_type, &res))
        return res;
    int mark = p->mark;
    { // lambda_name_with_default _loop0_83
        NameDefaultPair* elem;
        asdl_seq * seq;
        if (
            (elem = lambda_name_with_default_rule(p))
            &&
            (seq = _loop0_83_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_82_type, res);
    return res;
}

// _loop0_85: ',' (lambda_plain_name !'=')
static asdl_seq *
_loop0_85_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_85_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' (lambda_plain_name !'=')
        void *elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = _tmp_118_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_85");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_85");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_85_type, seq);
    return seq;
}

// _gather_84: (lambda_plain_name !'=') _loop0_85
static asdl_seq *
_gather_84_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_84_type, &res))
        return res;
    int mark = p->mark;
    { // (lambda_plain_name !'=') _loop0_85
        void *elem;
        asdl_seq * seq;
        if (
            (elem = _tmp_118_rule(p))
            &&
            (seq = _loop0_85_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_84_type, res);
    return res;
}

// _loop1_86: ('or' conjunction)
static asdl_seq *
_loop1_86_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_86_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ('or' conjunction)
        void *_tmp_119_var;
        while (
            (_tmp_119_var = _tmp_119_rule(p))
        )
        {
            res = _tmp_119_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_86");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_86");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_86_type, seq);
    return seq;
}

// _loop1_87: ('and' inversion)
static asdl_seq *
_loop1_87_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_87_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ('and' inversion)
        void *_tmp_120_var;
        while (
            (_tmp_120_var = _tmp_120_rule(p))
        )
        {
            res = _tmp_120_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_87");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_87");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_87_type, seq);
    return seq;
}

// _loop1_88: compare_op_bitwise_or_pair
static asdl_seq *
_loop1_88_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_88_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // compare_op_bitwise_or_pair
        CmpopExprPair* compare_op_bitwise_or_pair_var;
        while (
            (compare_op_bitwise_or_pair_var = compare_op_bitwise_or_pair_rule(p))
        )
        {
            res = compare_op_bitwise_or_pair_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_88");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_88");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_88_type, seq);
    return seq;
}

// _loop0_90: ',' slice
static asdl_seq *
_loop0_90_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_90_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' slice
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = slice_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_90");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_90");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_90_type, seq);
    return seq;
}

// _gather_89: slice _loop0_90
static asdl_seq *
_gather_89_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_89_type, &res))
        return res;
    int mark = p->mark;
    { // slice _loop0_90
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = slice_rule(p))
            &&
            (seq = _loop0_90_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_89_type, res);
    return res;
}

// _tmp_91: ':' expression?
static void *
_tmp_91_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_91_type, &res))
        return res;
    int mark = p->mark;
    { // ':' expression?
        void *d;
        void *literal;
        if (
            (literal = expect_token(p, 11))
            &&
            (d = expression_rule(p), 1)
        )
        {
            res = d;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_91_type, res);
    return res;
}

// _loop1_92: STRING
static asdl_seq *
_loop1_92_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_92_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // STRING
        expr_ty string_var;
        while (
            (string_var = string_token(p))
        )
        {
            res = string_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_92");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_92");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_92_type, seq);
    return seq;
}

// _tmp_93: tuple | group | genexp
static void *
_tmp_93_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_93_type, &res))
        return res;
    int mark = p->mark;
    { // tuple
        expr_ty tuple_var;
        if (
            (tuple_var = tuple_rule(p))
        )
        {
            res = tuple_var;
            goto done;
        }
        p->mark = mark;
    }
    { // group
        expr_ty group_var;
        if (
            (group_var = group_rule(p))
        )
        {
            res = group_var;
            goto done;
        }
        p->mark = mark;
    }
    { // genexp
        expr_ty genexp_var;
        if (
            (genexp_var = genexp_rule(p))
        )
        {
            res = genexp_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_93_type, res);
    return res;
}

// _tmp_94: list | listcomp
static void *
_tmp_94_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_94_type, &res))
        return res;
    int mark = p->mark;
    { // list
        expr_ty list_var;
        if (
            (list_var = list_rule(p))
        )
        {
            res = list_var;
            goto done;
        }
        p->mark = mark;
    }
    { // listcomp
        expr_ty listcomp_var;
        if (
            (listcomp_var = listcomp_rule(p))
        )
        {
            res = listcomp_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_94_type, res);
    return res;
}

// _tmp_95: dict | set | dictcomp | setcomp
static void *
_tmp_95_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_95_type, &res))
        return res;
    int mark = p->mark;
    { // dict
        expr_ty dict_var;
        if (
            (dict_var = dict_rule(p))
        )
        {
            res = dict_var;
            goto done;
        }
        p->mark = mark;
    }
    { // set
        expr_ty set_var;
        if (
            (set_var = set_rule(p))
        )
        {
            res = set_var;
            goto done;
        }
        p->mark = mark;
    }
    { // dictcomp
        expr_ty dictcomp_var;
        if (
            (dictcomp_var = dictcomp_rule(p))
        )
        {
            res = dictcomp_var;
            goto done;
        }
        p->mark = mark;
    }
    { // setcomp
        expr_ty setcomp_var;
        if (
            (setcomp_var = setcomp_rule(p))
        )
        {
            res = setcomp_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_95_type, res);
    return res;
}

// _tmp_96: star_named_expression ',' star_named_expressions?
static void *
_tmp_96_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_96_type, &res))
        return res;
    int mark = p->mark;
    { // star_named_expression ',' star_named_expressions?
        void *literal;
        expr_ty y;
        void *z;
        if (
            (y = star_named_expression_rule(p))
            &&
            (literal = expect_token(p, 12))
            &&
            (z = star_named_expressions_rule(p), 1)
        )
        {
            res = seq_insert_in_front ( p , y , z );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_96_type, res);
    return res;
}

// _tmp_97: yield_expr | named_expression
static void *
_tmp_97_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_97_type, &res))
        return res;
    int mark = p->mark;
    { // yield_expr
        expr_ty yield_expr_var;
        if (
            (yield_expr_var = yield_expr_rule(p))
        )
        {
            res = yield_expr_var;
            goto done;
        }
        p->mark = mark;
    }
    { // named_expression
        expr_ty named_expression_var;
        if (
            (named_expression_var = named_expression_rule(p))
        )
        {
            res = named_expression_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_97_type, res);
    return res;
}

// _loop0_99: ',' kvpair
static asdl_seq *
_loop0_99_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_99_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' kvpair
        KeyValuePair* elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = kvpair_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_99");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_99");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_99_type, seq);
    return seq;
}

// _gather_98: kvpair _loop0_99
static asdl_seq *
_gather_98_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_98_type, &res))
        return res;
    int mark = p->mark;
    { // kvpair _loop0_99
        KeyValuePair* elem;
        asdl_seq * seq;
        if (
            (elem = kvpair_rule(p))
            &&
            (seq = _loop0_99_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_98_type, res);
    return res;
}

// _loop1_100: (ASYNC? 'for' star_targets 'in' disjunction (('if' disjunction))*)
static asdl_seq *
_loop1_100_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop1_100_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // (ASYNC? 'for' star_targets 'in' disjunction (('if' disjunction))*)
        void *_tmp_121_var;
        while (
            (_tmp_121_var = _tmp_121_rule(p))
        )
        {
            res = _tmp_121_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop1_100");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    if (n == 0) {
        PyMem_Free(children);
        return NULL;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop1_100");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop1_100_type, seq);
    return seq;
}

// _tmp_101: ',' args
static void *
_tmp_101_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_101_type, &res))
        return res;
    int mark = p->mark;
    { // ',' args
        expr_ty c;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (c = args_rule(p))
        )
        {
            res = c;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_101_type, res);
    return res;
}

// _tmp_102: ',' args
static void *
_tmp_102_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_102_type, &res))
        return res;
    int mark = p->mark;
    { // ',' args
        expr_ty c;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (c = args_rule(p))
        )
        {
            res = c;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_102_type, res);
    return res;
}

// _loop0_104: ',' kwarg
static asdl_seq *
_loop0_104_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_104_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' kwarg
        KeywordOrStarred* elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = kwarg_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_104");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_104");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_104_type, seq);
    return seq;
}

// _gather_103: kwarg _loop0_104
static asdl_seq *
_gather_103_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_103_type, &res))
        return res;
    int mark = p->mark;
    { // kwarg _loop0_104
        KeywordOrStarred* elem;
        asdl_seq * seq;
        if (
            (elem = kwarg_rule(p))
            &&
            (seq = _loop0_104_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_103_type, res);
    return res;
}

// _loop0_105: (',' star_target)
static asdl_seq *
_loop0_105_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_105_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // (',' star_target)
        void *_tmp_122_var;
        while (
            (_tmp_122_var = _tmp_122_rule(p))
        )
        {
            res = _tmp_122_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_105");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_105");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_105_type, seq);
    return seq;
}

// _loop0_107: ',' star_target
static asdl_seq *
_loop0_107_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_107_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' star_target
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = star_target_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_107");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_107");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_107_type, seq);
    return seq;
}

// _gather_106: star_target _loop0_107
static asdl_seq *
_gather_106_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_106_type, &res))
        return res;
    int mark = p->mark;
    { // star_target _loop0_107
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = star_target_rule(p))
            &&
            (seq = _loop0_107_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_106_type, res);
    return res;
}

// _loop0_109: ',' del_target
static asdl_seq *
_loop0_109_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_109_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' del_target
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = del_target_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_109");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_109");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_109_type, seq);
    return seq;
}

// _gather_108: del_target _loop0_109
static asdl_seq *
_gather_108_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_108_type, &res))
        return res;
    int mark = p->mark;
    { // del_target _loop0_109
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = del_target_rule(p))
            &&
            (seq = _loop0_109_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_108_type, res);
    return res;
}

// _loop0_111: ',' target
static asdl_seq *
_loop0_111_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_111_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ',' target
        expr_ty elem;
        void *literal;
        while (
            (literal = expect_token(p, 12))
            &&
            (elem = target_rule(p))
        )
        {
            res = elem;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_111");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_111");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_111_type, seq);
    return seq;
}

// _gather_110: target _loop0_111
static asdl_seq *
_gather_110_rule(Parser *p)
{
    asdl_seq * res = NULL;
    if (is_memoized(p, _gather_110_type, &res))
        return res;
    int mark = p->mark;
    { // target _loop0_111
        expr_ty elem;
        asdl_seq * seq;
        if (
            (elem = target_rule(p))
            &&
            (seq = _loop0_111_rule(p))
        )
        {
            res = seq_insert_in_front(p, elem, seq);
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _gather_110_type, res);
    return res;
}

// _tmp_112: star_targets '='
static void *
_tmp_112_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_112_type, &res))
        return res;
    int mark = p->mark;
    { // star_targets '='
        void *literal;
        expr_ty z;
        if (
            (z = star_targets_rule(p))
            &&
            (literal = expect_token(p, 22))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_112_type, res);
    return res;
}

// _tmp_113: '.' | '...'
static void *
_tmp_113_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_113_type, &res))
        return res;
    int mark = p->mark;
    { // '.'
        void *literal;
        if (
            (literal = expect_token(p, 23))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    { // '...'
        void *literal;
        if (
            (literal = expect_token(p, 52))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_113_type, res);
    return res;
}

// _tmp_114: '.' | '...'
static void *
_tmp_114_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_114_type, &res))
        return res;
    int mark = p->mark;
    { // '.'
        void *literal;
        if (
            (literal = expect_token(p, 23))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    { // '...'
        void *literal;
        if (
            (literal = expect_token(p, 52))
        )
        {
            res = literal;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_114_type, res);
    return res;
}

// _tmp_115: plain_name !'='
static void *
_tmp_115_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_115_type, &res))
        return res;
    int mark = p->mark;
    { // plain_name !'='
        arg_ty plain_name_var;
        if (
            (plain_name_var = plain_name_rule(p))
            &&
            lookahead_with_int(0, expect_token, p, 22)
        )
        {
            res = plain_name_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_115_type, res);
    return res;
}

// _tmp_116: '@' factor NEWLINE
static void *
_tmp_116_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_116_type, &res))
        return res;
    int mark = p->mark;
    { // '@' factor NEWLINE
        expr_ty f;
        void *literal;
        void *newline_var;
        if (
            (literal = expect_token(p, 49))
            &&
            (f = factor_rule(p))
            &&
            (newline_var = newline_token(p))
        )
        {
            res = f;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_116_type, res);
    return res;
}

// _tmp_117: ',' star_expression
static void *
_tmp_117_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_117_type, &res))
        return res;
    int mark = p->mark;
    { // ',' star_expression
        expr_ty c;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (c = star_expression_rule(p))
        )
        {
            res = c;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_117_type, res);
    return res;
}

// _tmp_118: lambda_plain_name !'='
static void *
_tmp_118_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_118_type, &res))
        return res;
    int mark = p->mark;
    { // lambda_plain_name !'='
        arg_ty lambda_plain_name_var;
        if (
            (lambda_plain_name_var = lambda_plain_name_rule(p))
            &&
            lookahead_with_int(0, expect_token, p, 22)
        )
        {
            res = lambda_plain_name_var;
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_118_type, res);
    return res;
}

// _tmp_119: 'or' conjunction
static void *
_tmp_119_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_119_type, &res))
        return res;
    int mark = p->mark;
    { // 'or' conjunction
        expr_ty c;
        void *keyword;
        if (
            (keyword = expect_token(p, 531))
            &&
            (c = conjunction_rule(p))
        )
        {
            res = c;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_119_type, res);
    return res;
}

// _tmp_120: 'and' inversion
static void *
_tmp_120_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_120_type, &res))
        return res;
    int mark = p->mark;
    { // 'and' inversion
        expr_ty c;
        void *keyword;
        if (
            (keyword = expect_token(p, 532))
            &&
            (c = inversion_rule(p))
        )
        {
            res = c;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_120_type, res);
    return res;
}

// _tmp_121: ASYNC? 'for' star_targets 'in' disjunction (('if' disjunction))*
static void *
_tmp_121_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_121_type, &res))
        return res;
    int mark = p->mark;
    { // ASYNC? 'for' star_targets 'in' disjunction (('if' disjunction))*
        expr_ty a;
        expr_ty b;
        asdl_seq * c;
        void *keyword;
        void *keyword_1;
        void *y;
        if (
            (y = async_token(p), 1)
            &&
            (keyword = expect_token(p, 517))
            &&
            (a = star_targets_rule(p))
            &&
            (keyword_1 = expect_token(p, 518))
            &&
            (b = disjunction_rule(p))
            &&
            (c = _loop0_123_rule(p))
        )
        {
            res = _Py_comprehension ( a , b , c , ( y == NULL ) ? 0 : 1 , p -> arena );
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_121_type, res);
    return res;
}

// _tmp_122: ',' star_target
static void *
_tmp_122_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_122_type, &res))
        return res;
    int mark = p->mark;
    { // ',' star_target
        expr_ty c;
        void *literal;
        if (
            (literal = expect_token(p, 12))
            &&
            (c = star_target_rule(p))
        )
        {
            res = c;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_122_type, res);
    return res;
}

// _loop0_123: ('if' disjunction)
static asdl_seq *
_loop0_123_rule(Parser *p)
{
    void *res = NULL;
    if (is_memoized(p, _loop0_123_type, &res))
        return res;
    int mark = p->mark;
    void **children = PyMem_Malloc(sizeof(void *));
    if (!children) {
        PyErr_Format(PyExc_MemoryError, "Parser out of memory");
        return NULL;
    }
    ssize_t children_capacity = 1;
    ssize_t n = 0;
    { // ('if' disjunction)
        void *_tmp_124_var;
        while (
            (_tmp_124_var = _tmp_124_rule(p))
        )
        {
            res = _tmp_124_var;
            if (n == children_capacity) {
                children_capacity *= 2;
                children = PyMem_Realloc(children, children_capacity*sizeof(void *));
                if (!children) {
                    PyErr_Format(PyExc_MemoryError, "realloc _loop0_123");
                    return NULL;
                }
            }
            children[n++] = res;
            mark = p->mark;
        }
        p->mark = mark;
    }
    asdl_seq *seq = _Py_asdl_seq_new(n, p->arena);
    if (!seq) {
        PyErr_Format(PyExc_MemoryError, "asdl_seq_new _loop0_123");
        return NULL;
    }
    for (int i = 0; i < n; i++) asdl_seq_SET(seq, i, children[i]);
    PyMem_Free(children);
    insert_memo(p, mark, _loop0_123_type, seq);
    return seq;
}

// _tmp_124: 'if' disjunction
static void *
_tmp_124_rule(Parser *p)
{
    void * res = NULL;
    if (is_memoized(p, _tmp_124_type, &res))
        return res;
    int mark = p->mark;
    { // 'if' disjunction
        void *keyword;
        expr_ty z;
        if (
            (keyword = expect_token(p, 510))
            &&
            (z = disjunction_rule(p))
        )
        {
            res = z;
            if (res == NULL && PyErr_Occurred()) {
                longjmp(p->error_env, 1);
            }
            goto done;
        }
        p->mark = mark;
    }
    res = NULL;
  done:
    insert_memo(p, mark, _tmp_124_type, res);
    return res;
}

mod_ty
parse_start(Parser *p)
{
    return start_rule(p);
}
