Add relaxed variants of the _Py_atomic_add and _Py_atomic_compare_exchange
functions. The existing operations are sequentially consistent, which is
stronger (and on ARM, measurably more expensive) than necessary for uses like
statistics counters and unique ID allocation, where the operation must be
atomic but does not need to order surrounding memory accesses.
