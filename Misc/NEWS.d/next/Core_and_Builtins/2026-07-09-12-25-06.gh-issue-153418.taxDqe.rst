Fix error handling in the ``FrameLocalsProxy`` returned by
:attr:`frame.f_locals`. Exceptions raised by the argument of ``update()``
or ``|=`` are now propagated instead of being replaced with
:exc:`TypeError` or :exc:`SystemError`, or aborting debug builds.
