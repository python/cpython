Make sure warning.showwarning and warning.formatwarning display
the log traceback, where the object has been allocated.

.. code-block:: python
  logging.captureWarnings(True) to show object allocation
