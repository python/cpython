Add a Unicode character browser to IDLE's Browse menu.
It browses Unicode blocks, searches for characters by name or code point,
and inserts a character into the window or copies it to the clipboard.
