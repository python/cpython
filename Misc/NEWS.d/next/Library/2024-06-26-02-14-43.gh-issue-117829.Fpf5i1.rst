Add flags `--include-pattern` and `--exclude-pattern` to the CLI of the :mod:`zipapp` module. 
These flags accept regex patterns to indicate allow-list and/or deny-list of files to be included in the zipapp file.
