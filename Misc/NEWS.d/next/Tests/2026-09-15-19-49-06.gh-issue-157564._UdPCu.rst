Fixed a crash in :mod:`sqlite3` when a ``text_factory`` or converter called
:meth:`~sqlite3.Cursor.execute` on the same cursor during fetch. A rejected
re-entrant ``execute()`` no longer clears the statement being fetched.
